### tokens — 25 findings

| # | File | Topic | Current | Target | Phase | Effort |
|---|---|---|---|---|---|---|
| 1 | `webapp/src/app/globals.css:70-93` | token-parity-core | All 10 design-board tokens already exist under the same names in :root — --z-ink:#0b0b0c, --z-white:#F6F0E3, --z-brass:#a98a51, --z-income:#5CB88A, --z-expense:#E8875A, --z-alert:#D4A843, --z-debt:#E05545, --z-sage-light:#D9CCB9, --z-sage-dark:#938C7E, --z-border:rgba(217,204,185,0.08). Extra webapp-only tokens: --z-olive-deep #3F4632, --z-sage #768053, --z-brass-hot #c2a063, --z-excellent #3D9E6E, --z-surface #161618, --z-surface-2 #1c1c1f, --z-surface-3 #242428, --z-border-strong rgba(217,204,185,0.16). All exposed as Tailwind utilities via @theme inline --color-z-* (lines 49-68) → text-z-brass, bg-z-income, border-z-debt, etc. | No new tokens needed (spec: 'cero hues nuevos'). Verdict/chartTheme bind to these existing vars. Note --z-excellent is a 5th status hue outside the board's 3-token verdict palette — Verdict must NOT use it. | 1 | trivial |
| 2 | `webapp/src/app/globals.css:243-247` | surface-recipe-exists | The exact 8%-tint/20%-border recipe the Verdict spec names already exists as utility classes in @layer base: `.surface-income { color: var(--z-income); border-color: color-mix(in srgb, var(--z-income) 20%, transparent); background: color-mix(in srgb, var(--z-income) 8%, transparent); }` plus .surface-expense, .surface-debt, .surface-alert, .surface-neutral (sage-dark). Only 3 consumer files today: src/components/subscriptions/subscription-row.tsx:39,44 (`<Badge variant="outline" className="surface-expense">` / `surface-alert`), src/components/settings/delete-account-section.tsx:51 (`surface-debt`), src/components/settings/reset-data-section.tsx:51 (comment only). | <Verdict /> pill binds to this exact recipe (spec T1: 'existing .surface-* recipe... No new styles'). Reuse the classes or the color-mix formula directly; states map vas-bien→surface-income, cerca/atencion→surface-alert, te-pasaste→surface-debt. | 1 | trivial |
| 3 | `webapp/src/components/mobile/v2/state-chip.tsx:3-28` | state-chip-closest-verdict-precursor | StateChip is the closest existing verdict chip but uses a DIFFERENT recipe: variants `sage: "bg-z-income/12 border-z-income/25 text-z-income"`, `brass: "bg-z-brass/12 border-z-brass/25 text-z-brass"`, `warn: "bg-z-alert/12 border-z-alert/25 text-z-alert"`, `danger: "bg-z-debt/12 border-z-debt/25 text-z-debt"` on `inline-flex items-center rounded-full border px-2.5 py-0.5 text-[9px] font-bold uppercase tracking-[0.06em]` — 12%/25% tint, UPPERCASE 9px, no icon, free-form label. Consumers: plan-tab-presupuesto.tsx, mobile/v2/deudas/debt-trend-card.tsx, mobile/v2/plan/plan-budget-hero.tsx, budget/scenario/scenario-commit.tsx. | Replaced by <Verdict /> (8% tint/20% border via surface-*, sentence case 12px/600, fixed Lucide icon 12px stroke 2.5, closed 4-word vocabulary). Also note it has a `brass` variant — spec forbids brass verdicts. | 1 | medium |
| 4 | `webapp/src/components/dashboard/status-headline.tsx:27-55` | dashboard-status-dot | Dashboard status dot + sentence: `dotColor = "bg-z-debt" \| "bg-z-alert" \| "bg-z-income"` rendered as `<span className={\`inline-block h-2 w-2 rounded-full shrink-0 ${dotColor}\`}>` beside 15px muted copy. Strings: "Te pasaste del presupuesto — {amount} por encima" (>100%), "Cerca del limite — queda poco margen este mes" (>=90%, note missing accent on 'limite'), "Vas bien este mes — gastos dentro del presupuesto". Thresholds: >100 / >=90 / else (spec wants 75-99 for cerca). | Phase 1 converges this into <Verdict /> chip in the same hero position; existing sentence survives verbatim as the detail line. Delete the dot. | 1 | small |
| 5 | `webapp/src/components/plan/tabs/plan-tab-presupuesto.tsx:149-155` | presupuesto-pill-dialect | Budget hero chip dialect: `pressure = progress >= 100 ? "critical" : progress >= 80 ? "watch" : "stable"` with `chipConfig = { stable: { label: "En control", variant: "sage" }, watch: { label: "Atención", variant: "warn" }, critical: { label: "Sobre límite", variant: "danger" } }` rendered via <StateChip /় (line ~190). Hero number below is `text-[42px] font-[680] leading-none tracking-[-0.06em]` colored text-z-debt/text-z-income. Related: src/components/budget/mobile-budget-list.tsx:97-103 section labels "Sobre límite" (text-z-expense), "Cerca del límite" (text-z-brass — brass carrying a verdict, banned), "Dentro del límite" (text-z-income). | Vocabulary closes to Vas bien · Cerca del límite · Te pasaste · Atención via <Verdict />; "En control"/"Sobre límite" labels deleted; brass never on verdicts. | 1 | medium |
| 6 | `webapp/src/components/ui/chip.tsx:19-65` | chip-primitive-conventions | Canonical ACTION chip (not status): cva variants neutral (`border-white/6 bg-white/[0.03] text-muted-foreground hover:bg-white/[0.06]`), active (`border-z-brass/30 bg-z-brass/10 text-z-brass`), primary (`border-z-brass bg-z-brass text-z-ink font-semibold`), danger (`border-z-expense/25 bg-z-expense/5 text-z-expense`); sizes sm `px-2.5 py-1 text-[11px]` / md `px-3 py-1.5 text-xs`; asChild via radix Slot; data-slot="chip" data-variant attrs; icons auto-sized `[&>svg]:size-[13px]`. | Keep for actions. <Verdict /> should be a NEW sibling primitive (ui/verdict.tsx) following this exact pattern: cva + VariantProps, data-slot, asChild optional, cn() from @/lib/utils — spec chip anatomy h-24px, pad 0 10px 0 8px, radius 999, 12px/600. | 1 | small |
| 7 | `webapp/src/components/ui/badge.tsx:7-27` | badge-primitive | shadcn Badge cva: variants default (bg-primary), secondary, destructive (bg-destructive text-white), outline (border-border text-foreground), ghost, link; base `inline-flex items-center justify-center rounded-full border border-transparent px-2 py-0.5 text-xs font-medium ... [&>svg]:size-3`. Status usage today combines `variant="outline"` + surface-* class (subscription-row.tsx:39,44). | Keep Badge for generic labels; the Badge+surface-* combo call sites migrate to <Verdict compact /> where the label is a status judgment. | 1 | trivial |
| 8 | `webapp/src/lib/constants/styles.ts:126-155` | other-pill-inventory | Non-status pill constants that Verdict must NOT absorb (actions/toggles): MOBILE_ACTION_BUTTON_CLASS `rounded-lg border border-z-brass/20 bg-z-brass/8 px-2.5 py-1 text-[10px] font-semibold text-z-brass` (126-127), DETAIL_ACTION_CHIP_CLASS (131-132), SEGMENTED_TAB_CLASS / SEGMENTED_TAB_ACTIVE_CLASS `border-z-brass/30 bg-z-brass/15 text-z-brass` (134-140), CHIP_NEUTRAL_CLASS `rounded-full border-white/6 bg-white/[0.03] px-3 py-1.5 text-xs` (143-144), chipToggleClass selected `border-z-brass bg-z-brass text-z-ink` (147-155). Also ui/select-pill.tsx:21-26 SelectPill active `border-z-brass/40 bg-z-brass/10 text-z-brass`, and ui/page-hero.tsx HeroPill (line 70-79, `rounded-full border-white/6 bg-black/10 px-3 py-1 text-[11px] uppercase tracking-[0.18em] text-z-sage-light`) + HeroAccentPill (89-98, `border-z-brass/30 bg-z-brass/10 text-z-brass`). | Unchanged — these are action/toggle pills. Inventory recorded so Verdict migration doesn't touch them. | 1 | trivial |
| 9 | `webapp/src/components/ui/kpi-widget.tsx:14-20` | kpi-widget-tint-drift | KPIWidget colorMap uses a 10% icon-tint dialect: `income: { icon: "bg-z-income/10 text-z-income" ... }`, expense/debt/alert same pattern, on `rounded-[14px] bg-z-surface-2` container (no border-white/6, arbitrary radius). | Not a Verdict target, but a tint-recipe drift call-site (10% vs the 8%/20% surface recipe) to normalize during Phase 3 density pass. | 3 | small |
| 10 | `webapp/src/app/globals.css:256-262` | motion-tokens-missing | NO motion tokens exist anywhere (`--motion` greps to zero across src/ and docs/). Only motion CSS in globals.css: `@keyframes page-enter { from { opacity: 0; transform: translateY(3px); } ... }` + `.animate-page-enter { animation: page-enter 0.1s ease-out; }`. tw-animate-css is imported (line 2). Hardcoded Tailwind durations across components: duration-200 ×26, duration-150 ×20, duration-300 ×5, duration-500 ×1, duration-400 ×1, duration-75 ×1 — all with default easings (ease-out etc.). | Add to :root — `--motion-ease: cubic-bezier(0.2, 0, 0, 1); --motion-1: 120ms; --motion-2: 160ms; --motion-3: 200ms` — and migrate transition call-sites; anything >200ms deleted except the 600ms celebration ring sweep. | 2 | medium |
| 11 | `webapp/src/components/mobile/v2/expand.tsx:16-26` | disclosure-precursor-expand | Shared Expand already uses the spec's mechanism: `"grid transition-[grid-template-rows] duration-200 ease-out"`, `open ? "grid-rows-[1fr]" : "grid-rows-[0fr]"`, inner `min-h-0 overflow-hidden`, content stays mounted (AnimatedAccordion rule). No caret, no label, wrong duration/curve (200ms ease-out vs 160ms --motion-ease), no 120ms content-enter. | Becomes the engine of <Disclosure /> — add caret (chevron-down 12px stroke 1.5, rotate 180°), "Ver + qué"/"Ocultar …" label 12px/600 sage-light, --motion-2 container + --motion-1 content opacity/translateY(-3px). | 2 | medium |
| 12 | `webapp/src/components/mobile/cards/expandable-card.tsx:24-52` | disclosure-precursor-expandable-card | ExpandableCard: container `rounded-[18px] border border-white/6 bg-[#111] transition-colors` (+`border-white/10` when expanded), inline `style={{ gridTemplateRows: expanded ? "1fr" : "0fr" }}` with `transition-[grid-template-rows] duration-200 ease-out`, content fade `transition-opacity duration-150 ... delay-75`. Silent grower: aria-expanded but no caret/label. Other inline grid-rows expanders: dashboard/burndown-expandable.tsx, mobile/cards/mobile-hero-card.tsx, mobile-alert-card.tsx, mobile-upcoming-payments.tsx, mobile/v2/linked-metric-detail-panel.tsx, mobile/v2/deudas/deudas-salary-bar.tsx, deudas-hero.tsx, mobile/v2/plan/plan-zone-chips.tsx. | All expand call-sites converge on <Disclosure /> with caret + labeled affordance and motion tokens; rounded-[18px] and bg-[#111] are the Tier-2 compact-card recipe (see card-tiers finding). | 2 | large |
| 13 | `webapp/src/lib/constants/styles.ts:157-163` | expand-toggle-brass-violation | INLINE_EXPAND_TOGGLE_CLASS = `"flex w-full items-center justify-center gap-1 border-t border-white/6 py-2 text-[11px] font-semibold text-z-brass transition-colors hover:bg-white/[0.02]"` — a disclosure affordance in BRASS (spec: never brass, sage-light→white). ROW_EXPAND_TRIGGER_CLASS = `"flex w-full items-center gap-2.5 py-2.5 text-left transition-colors hover:bg-white/[0.02]"` (silent, no caret/label). Consumers use labels like "Ver todas / Ver menos" (bare-ish). | Both fold into <Disclosure /> affordance: 12px/600 text-z-sage-light hover:text-white, chevron caret, "Ver + qué"/"Ocultar + qué" labels; brass removed. | 2 | small |
| 14 | `webapp/src/components/ui/collapsible.tsx:1-33` | radix-collapsible-unstyled | Unstyled Radix Collapsible passthrough (Collapsible/CollapsibleTrigger/CollapsibleContent with data-slot attrs, zero classes). Available but rarely the pattern used; grid-rows Expand is the de-facto idiom. | Either becomes the a11y skeleton inside <Disclosure /> or stays for non-animated collapse; decide during Phase 2 build. | 2 | trivial |
| 15 | `webapp/src/components/month-selector.tsx:16-76` | month-selector-current-state | MonthSelector: shadcn `<Button variant="outline" size="icon-sm">` chevrons (ChevronLeft/Right h-4 w-4), center Button `variant={isCurrent ? "secondary" : "outline"}` with `compact ? "min-w-0 px-2 text-xs capitalize" : "min-w-[120px] sm:min-w-[160px] capitalize"`; compact mode renders formatMonthLabelShort → "Abr" (the 'Abr pill' the spec deletes). Month IS URL state (`?month=` param, router.replace, deletes param at current month, resets page) but per-page — searchParams don't follow across tab switches. Next chevron `disabled={isCurrent}`. Labels from src/lib/utils/date.ts:124-131 — formatMonthLabel `format(date, "MMMM yyyy", { locale: es })` (lowercase, capitalized via CSS `capitalize`), formatMonthLabelShort `"MMM"`. | <MonthControl />: compact-card container (#111/border-white/6/radius 10), chevrons 14px stroke 1.5 hit 32×44, fixed-width tabular label min 88px, "Abril 2026" always (kill compact/short mode from headers; "abr" reserved for chart axes), month key shared across the five surfaces (layout-level searchParam propagation). | 2 | medium |
| 16 | `webapp/src/components/ui/page-header-row.tsx:19-45` | header-grammar-primitives | PageHeaderRow (desktop-only `hidden lg:flex`): `<h1 className="text-[22px] font-bold tracking-tight">` + optional subtitle `mt-0.5 text-sm text-muted-foreground` + actions slot. PageHero (ui/page-hero.tsx:29-59) renders title `text-2xl font-semibold tracking-tight text-z-white lg:text-3xl` + mandatory `description` paragraph + pills row — the title+subtitle dialect the spec deletes. TOKENS.md §2 documents page title as text-2xl/text-3xl font-semibold tracking-tight. | Canonical header: H1 22px/600 (28px/700 desktop) sentence case matching tab name, MonthControl top-right, NO subtitles. PageHeaderRow drops subtitle prop; PageHero description demoted per-surface. | 2 | medium |
| 17 | `webapp/src/lib/constants/styles.ts:63-89` | card-tiers-mapping | No z-card-* classes exist anywhere (grep zero). Equivalents: Tier 1 surface = PANEL_SURFACE_CLASS `"rounded-2xl border border-white/6 bg-z-surface-2/80 shadow-[inset_0_1px_0_rgba(255,255,255,0.03)]"` (63-64) + PANEL_SURFACE_SUBTLE_CLASS bg-z-surface-2/55 (67-68) + MOBILE_CARD_CLASS/-TIGHT (86-89). Tier 2 compact = TOKENS.md §3 `rounded-xl border border-white/6 bg-[#111] px-3 py-2`; bg-[#111] hardcoded in 5 files: personas/persona-card.tsx, mobile/cards/expandable-card.tsx (rounded-[18px] variant), mobile/v2/deudas/debt-trend-card.tsx, deudas-cuentas-lens.tsx, detail-cell.tsx. Tier 3 stat = `rounded-2xl border border-white/6 bg-black/10 p-4` via StatCard + CompactMetricBox (ui/stat-card.tsx:22,55) and PANEL_INSET_CLASS/-INTERACTIVE/-SUBTLE (75-83). shadcn ui/card.tsx is the generic `bg-card rounded-xl border py-6` tier used by desktop surfaces. SummaryCard (ui/summary-card.tsx:20-22) repeats the Tier-1 recipe inline. | Board's z-card-surface/z-card-compact/z-card-stat map onto PANEL_SURFACE_CLASS / Tier-2 #111 compact / PANEL_INSET_CLASS respectively; MonthControl + compact rows use the Tier-2 recipe (#111, border white/6, radius 10). | 2 | small |
| 18 | `webapp/src/lib/constants/styles.ts:71-72` | hero-gradient-exact-match | HERO_CARD_GRADIENT_CLASS = `"bg-[radial-gradient(circle_at_top_left,rgba(63,70,50,0.22),transparent_42%),linear-gradient(180deg,rgba(27,30,27,0.96),rgba(18,20,18,0.98))]"` — EXACTLY the board's hero gradient. Used by: mobile/cards/mobile-hero-card.tsx, mobile/v2/plan/mobile-recurrentes-view.tsx, mobile/v2/inicio/pulse-widget.tsx, mobile/v2/plan/plan-net-hero.tsx. A drifted sibling lives in ui/page-hero.tsx:3-7 GRADIENTS: sage uses rgba(63,70,50,0.26) alpha and brass uses rgba(147,120,68,0.16) — two near-duplicates of the same recipe. | HERO_CARD_GRADIENT_CLASS is the canonical hero-card gradient for the fixed hero slot (eyebrow → number → verdict → detail → meta); consider collapsing PageHero GRADIENTS.sage into it. | 1 | trivial |
| 19 | `webapp/src/lib/constants/styles.ts:1-11` | button-constants-and-stale-tokens-doc | BRASS_BUTTON_CLASS = `"bg-gradient-to-b from-z-brass-hot to-z-brass text-z-ink hover:brightness-110"`; GHOST_BUTTON_CLASS = `"border-white/6 bg-black/10 text-z-sage-light hover:bg-white/5 hover:text-z-sage-light"`; BRASS_GHOST_BUTTON_CLASS = `"border-z-brass/20 bg-z-brass/8 text-z-brass hover:bg-z-brass/12"`. docs/design-system/TOKENS.md §7 (lines 111-115) is STALE: documents `BRASS_BUTTON_CLASS = "bg-z-brass text-z-ink hover:bg-z-brass/90"` and `GHOST_BUTTON_CLASS = "border-white/8 ..."` — neither matches code. Also SECTION_EYEBROW_CLASS (styles.ts:59-60) = `"text-[10px] font-semibold uppercase tracking-[0.18em] text-z-sage-dark"` matches the board's eyebrow spec exactly (reuse for hero slot + celebration eyebrows). | Buttons unchanged (brass = actions, per spec). Update TOKENS.md §7 to match code during the unification-layer docs pass; celebration CTA 'brass-ghost' = existing BRASS_GHOST_BUTTON_CLASS. | 1 | trivial |
| 20 | `webapp/src/app/globals.css:182-187` | chart-token-rebind | Chart slots bound in :root (183-187) AND duplicated in .dark (220-224): `--chart-1: var(--z-income); --chart-2: var(--z-expense); --chart-3: var(--z-alert); --chart-4: var(--z-debt); --chart-5: var(--z-brass);` — components reference them (e.g. dashboard/burn-rate-card.tsx:193 `stroke="var(--chart-1)"`). | T2 rebind: --chart-1 → --z-brass (actual/tú), --chart-2 → --z-sage-dark (ideal/promedio), --chart-3 → --z-income, --chart-4 → --z-expense, --chart-5 → --z-debt. Must change BOTH :root and .dark blocks, then audit every var(--chart-N) consumer since semantics flip (chart-1 currently means income-green, will mean brass). | 3 | medium |
| 21 | `webapp/src/components/ui/chart.tsx:37-73` | chart-theme-home | shadcn ChartContainer/ChartStyle wrapper exists (recharts, per-chart ChartConfig with color/theme per series); no shared zeta chartTheme object — each chart in src/components/charts/ configures its own grid/axis/palette. Some already follow spec DNA piecemeal (burn-rate-card.tsx:195 `strokeDasharray="4 3"` forecast + `isAnimationActive={false}`, labels `fill: "var(--z-sage-dark)" fontSize: 9`). One 360° SVG ring exists at src/components/debt/debt-quick-stats.tsx:36-42 (48px circle, strokeDasharray/strokeDashoffset, strokeLinecap="round") — precursor of the Tier-1 270° gauge; no 128-viewBox gauge exists yet. | New `chartTheme` object (axisLine={false}, tickLine={false}, CartesianGrid vertical={false} stroke white/6, isAnimationActive:false, series slots) exported near ui/chart.tsx or lib/, consumed by Tendencias/forecast/income-expense charts; hand-rolled 270° gauge component (viewBox 0 0 128 128, pathLength=100) as new primitive. | 3 | large |
| 22 | `docs/design-system/TOKENS.md:62-77` | tokens-doc-progress-bar-conflict | TOKENS.md §4 canonical progress bar: container `h-2 w-full rounded-full bg-muted`, thresholds `>=100% → bg-red-500`, `>=75% → bg-yellow-500`, `<75% → bg-emerald-500` — raw Tailwind hues, not z-tokens, and h-2 (8px). §6 hero metric documented as `text-[32px] font-extrabold tabular-nums` (board says 34px/800 ls -0.02em); §10 maps color semantics to text-emerald-500/text-red-500/text-amber-400 alternates. | T6 row bars: 3px hairlines, track white/6, over-limit tint --z-debt, healthy fill white/25 (never brass, never green fills); TOKENS.md §4/§6/§10 need updating as part of the unification docs so the old recipe stops propagating. | 3 | small |
| 23 | `webapp/src/lib/health-levels.ts:5-67` | health-levels-5-state-system | Parallel status vocabulary: `type Level = "excelente" \| "solido" \| "atento" \| "alto" \| "critico"` with getLevelColor → var(--z-excellent)/var(--z-income)/var(--z-alert)/var(--z-expense)/var(--z-debt) (5 hues) and getLevelTag → "EXCELENTE"/"SÓLIDO"/"ATENTO"/"ALTO"/"CRÍTICO" uppercase tags. Feeds dashboard health meters. | Out of Verdict's closed vocabulary but a second status dialect; Phase 1 should decide whether health meters keep their 5-level gradient (analytical, not verdict) or their TAGS re-case into Verdict compact rows. Flag: uppercase tags violate copy rule 1 if rendered as verdicts. | 1 | medium |
| 24 | `webapp/src/app/globals.css:256-262` | celebration-keyframes-missing | Only `@keyframes page-enter` exists. No z-ring-sweep (dashoffset 100→0, 600ms) or z-rise (opacity 0→1 + translateY 6px→0, 120ms) keyframes; no brass-on-number pattern anywhere (hero numbers today are white or semantic-colored, e.g. plan-tab-presupuesto.tsx:196-199 `text-[42px] font-[680] ... text-z-debt\|text-z-income`) — good: brass-on-number is unclaimed and can be reserved for celebrations. | Phase 4 adds @keyframes z-ring-sweep + z-rise to globals.css next to page-enter; brass number treatment appears only in the three celebration events. | 4 | small |
| 25 | `webapp/src/components/ui/chip.tsx:1-65` | primitive-conventions | Conventions for new primitives (Verdict, Disclosure, MonthControl, GroupSummary): live in webapp/src/components/ui/ as kebab-case .tsx files; named function exports (no default); `cn()` from "@/lib/utils" (clsx + twMerge, webapp/src/lib/utils.ts:1-6); variantful components use cva + `VariantProps` + `data-slot`/`data-variant` attrs + radix Slot `asChild`; presentational ones are plain functions with className passthrough (SectionEyebrow pattern, ui/section-eyebrow.tsx:8-22 wrapping SECTION_EYEBROW_CLASS); colocated Storybook stories `*.stories.tsx` (41 existing — new primitives need stories per zetas-front-guy gate); shared class-string tokens go in lib/constants/styles.ts as UPPER_SNAKE *_CLASS exports with JSDoc. | New primitives follow these exact conventions: ui/verdict.tsx, ui/disclosure.tsx, ui/month-control.tsx (or evolve components/month-selector.tsx), ui/group-summary.tsx + stories. | 1 | trivial |

### verdicts — 35 findings

| # | File | Topic | Current | Target | Phase | Effort |
|---|---|---|---|---|---|---|
| 1 | `webapp/src/components/dashboard/status-headline.tsx:25-57` | dashboard-status-dot | The dashboard status dot: `<span className={"inline-block h-2 w-2 rounded-full shrink-0 " + dotColor} aria-label=... />` + 15px muted sentence. States: spentPercent > 100 → dotColor "bg-z-debt", copy `Te pasaste del presupuesto — ${formatCurrency(overAmount)} por encima`; spentPercent >= 90 → "bg-z-alert", copy "Cerca del limite — queda poco margen este mes" (note: 'limite' unaccented); else "bg-z-income", copy "Vas bien este mes — gastos dentro del presupuesto". spentPercent = (needs+wants)/income*100 from AllocationData. Null-data fallback: "Importa tu extracto para ver tu estado". | Replace dot+sentence with <Verdict state> chip in same position; existing sentence survives verbatim as the detail line. Thresholds re-bound to vas-bien / cerca 75-99 / te-pasaste >=100. | 1 | small |
| 2 | `webapp/src/components/dashboard/dashboard-hero.tsx:190-193` | dashboard-status-dot-mount | `<StatusHeadline allocationData={allocationData} />` rendered inside desktop DashboardHero below the KPI grid, wrapped with debtFreeBanner in a `space-y-3` div. Hero slot order today: 'Estado del mes' pill + freshness badge → eyebrow 'Disponible {windowLabel}' → 4xl/5xl number → guidanceCopy sentence → CTA buttons → KPI grid → StatusHeadline (verdict is at the BOTTOM, not under the number). Freshness badge at lines 37-42 is its own status dialect: fresh 'Base al día' (border-z-income/30 bg-z-income/10 text-z-income), stale 'Revisión recomendada' (z-alert), outdated 'Necesita actualización' (z-debt). | Verdict moves into the fixed hero slot: eyebrow → number → verdict → detail → meta. Freshness badge either becomes meta or feeds the atencion state. | 1 | medium |
| 3 | `webapp/src/components/dashboard/hybrid-hero.tsx:128-155` | inicio-hero-status-pill | HybridHero (Inicio hero, mounted in zones/hero-zone.tsx:83 desktop and zones/mobile-zone.tsx:155 mobile) renders a status pill top-right: `inline-flex shrink-0 items-center gap-1.5 rounded-full px-2.5 py-1 text-[10px] font-semibold uppercase tracking-[0.12em] bg-white/[0.04] border border-white/6` + `size-1.5 rounded-full` tone dot + status.label from deriveRitmoStatus ("Vas bien" \| "Vas justo" \| "Te pasaste") — CSS uppercase makes it read VAS BIEN etc. Fallback pill "Sin datos aún" (text-z-sage-dark). Tone maps at lines 39-55: TONE_TEXT_CLASS {income: text-z-income, alert: text-z-alert, debt: text-z-debt}, TONE_BG_CLASS (bg-*), TONE_STROKE {income: var(--color-z-sage), alert: var(--color-z-alert), debt: var(--color-z-debt)}. Expand button line 247: "Ver detalle"/"Ocultar detalle" (bare-adjacent label, T3 concern). | Pill becomes <Verdict /> chip: sentence case, 12px/600, icon per state, 8%/20% surface recipe; moves from top-right corner into the slot under the hero number; "Vas justo" is off-vocabulary → "Cerca del límite". | 1 | medium |
| 4 | `packages/shared/src/utils/ritmo.ts:43-51` | ritmo-verdict-logic | deriveRitmoStatus — the state machine feeding the Inicio hero pill (shared web+mobile): `if (ritmo.availableTotal < 0 \|\| overspentToday) return { tone: "debt", label: "Te pasaste" }; if (ritmo.spentFraction >= 0.85) return { tone: "alert", label: "Vas justo" }; return { tone: "income", label: "Vas bien" }` where overspentToday = spentToday > availablePerDay. Tones typed RitmoStatusTone = "income" \| "alert" \| "debt" — exactly the 3 tokens the Verdict spec uses, but 4th state (atencion) missing and label "Vas justo" off-vocabulary. cerca threshold is 0.85, spec says 0.75. | Return Verdict states (vas-bien \| cerca \| te-pasaste \| atencion) with closed vocabulary; "Vas justo" → "Cerca del límite"; threshold 75-99%. Shared package change — mobile consumes it too. | 1 | small |
| 5 | `webapp/src/components/plan/tabs/plan-tab-presupuesto.tsx:149-155, 186-233` | presupuesto-hero-pill | Presupuesto mobile hero (route /plan?tab=presupuesto, /presupuesto redirects here): `const pressure = progress >= 100 ? "critical" : progress >= 80 ? "watch" : "stable"` + chipConfig `{ stable: { label: "En control", variant: "sage" }, watch: { label: "Atención", variant: "warn" }, critical: { label: "Sobre límite", variant: "danger" } }` rendered via `<StateChip label={chip.label} variant={chip.variant} />` (line 191) — StateChip's uppercase class turns these into the "SOBRE LÍMITE"/"ATENCIÓN" stamps the handoff names. Hero number line 195-201: 42px `progress >= 100 ? "text-z-debt" : "text-z-income"` percent. Progress bar 214-224: `bg-gradient-to-r from-z-debt/90 to-z-debt/65` when >=100 else from-z-income gradients. | Single hero <Verdict />: "Sobre límite" re-cases to "Te pasaste", binds to surface-debt recipe; "En control" → "Vas bien"; threshold watch 80 → cerca 75. Hero becomes the one full-volume verdict of the screen. | 1 | medium |
| 6 | `webapp/src/components/mobile/v2/state-chip.tsx:3-28` | state-chip-primitive | StateChip — the uppercase pill primitive behind every SOBRE LÍMITE/ATENCIÓN stamp: `inline-flex items-center rounded-full border px-2.5 py-0.5 text-[9px] font-bold uppercase tracking-[0.06em]` with variants `{ sage: "bg-z-income/12 border-z-income/25 text-z-income", brass: "bg-z-brass/12 border-z-brass/25 text-z-brass", warn: "bg-z-alert/12 border-z-alert/25 text-z-alert", danger: "bg-z-debt/12 border-z-debt/25 text-z-debt" }`. Call-sites: plan-tab-presupuesto.tsx:191, plan-budget-hero.tsx:106, debt-trend-card.tsx:118, scenario-commit.tsx:110/119/125. Has a brass variant — brass carrying status violates the guardrail. | Verdict-bearing call-sites migrate to <Verdict /> (12px/600 sentence case, 8%/20% recipe, icon). StateChip either deleted or restricted to non-verdict metadata chips; brass variant never for status. | 1 | medium |
| 7 | `webapp/src/components/budget/mobile-budget-list.tsx:33-45, 96-104, 181-207` | budget-risk-groups | Mobile budget list groups by risk: `over` percentUsed > 100, `near` >= 85 && <= 100, `safe` < 85 (worst-first sort). Rendered as three RiskSections with colored eyebrow labels: `<RiskSection label="Sobre límite" labelClass="text-z-expense" .../>`, `label="Cerca del límite" labelClass="text-z-brass"`, `label="Dentro del límite" labelClass="text-z-income"` — note expense (orange) for over instead of debt, and brass for near. Section header = SECTION_EYEBROW_CLASS + count. All three groups render expanded at equal volume; healthy rows are not collapsed. | T6 GroupSummary V2 headers ("7 sobre límite · −$486.000" / "10 en orden"), state word once per group, healthy group collapses behind Disclosure "Ver 10 en orden"; colors re-bound: over → debt, near → alert (never brass). | 3 | medium |
| 8 | `webapp/src/components/budget/mobile-budget-list.tsx:209-268` | budget-row-stamps | BudgetCategoryRow: `const isOver = pct > 100; const isWarning = pct >= 85 && pct <= 100; const barColor = isOver ? "bg-z-debt" : isWarning ? "bg-z-alert" : "bg-z-income"`. Spent amount colored `isOver ? "text-z-debt" : isWarning ? "text-z-alert" : "text-foreground"` at text-xs; evidence "/ {formatCurrency(budget)}" 10px muted. Bar h-1.5 track bg-white/5, healthy fill bg-z-income (colored, spec says healthy = white/25). | Rows demote to V3/V4: delta only in group color, no per-row verdict tint on healthy rows; bars 3px hairline track white/6, only over-limit rows tint debt. | 3 | small |
| 9 | `webapp/src/components/budget/budget-summary-bar.tsx:49-59` | budget-desktop-percent-stamps | Desktop Presupuesto header percent: `overallPercent > 100 ? "text-z-debt" : overallPercent > 80 ? "text-z-expense" : "text-foreground"` on an 18px number — a naked colored percentage with no word (the '75-99% yellow percentages with no word' dialect, except it uses expense-orange at >80). | Percent stays V1 hero number; judgment moves to a single <Verdict /> chip; threshold 80 → 75, expense-orange → alert token. | 1 | small |
| 10 | `webapp/src/components/budget/budget-category-card.tsx:38-44` | budget-card-bar-thresholds | Desktop category card bar: `const barColor = totalPercent >= 100 ? "bg-z-debt" : totalPercent >= 75 ? "bg-z-alert" : undefined` (undefined falls back to category.color fill). The only place already using the 75% cerca threshold. | Keep thresholds (they match the spec) but bars become 3px hairlines; healthy fill white/25 not category color at full saturation. | 3 | trivial |
| 11 | `webapp/src/components/plan/plan-hero.tsx:19-32, 49-51` | plan-hero-pressure-badge | Desktop Plan hero (mounted plan-resumen-zone.tsx:36) pressure pill: `pressureStyles = { stable: { badge: "border-z-income/30 bg-z-income/10 text-z-income", label: "Estable" }, watch: { badge: "border-z-alert/30 bg-z-alert/10 text-z-alert", label: "Atención" }, critical: { badge: "border-z-debt/30 bg-z-debt/10 text-z-debt", label: "Crítico" } }` rendered `rounded-full border px-3 py-1 text-[11px] font-medium`. Hero slot today: eyebrow pill "Plan del mes" + pressure badge + month pill → eyebrow "Margen actual" → 4xl/5xl number (text-z-debt if negative) → h1 summary.headline → guidance paragraph. "Estable"/"Crítico" are off-vocabulary. | Badge row replaced by <Verdict /> under the hero number (full slot: eyebrow → number → verdict → detail → meta). Crítico → "Te pasaste", Estable → "Vas bien". | 1 | medium |
| 12 | `webapp/src/actions/plan.ts:88-163` | plan-pressure-logic | buildHeroSummary — Plan pressure state machine: `isCritical = heroData.availableToSpend < 0 \|\| (spentPercent != null && spentPercent > 100)`; `isWatch = !isCritical && (pendingObligations.length > 0 \|\| freshness !== "fresh" \|\| spentPercent >= 90)`; else stable. Emits headline strings ("Tu plan necesita un ajuste inmediato", "Tu plan está vivo, pero hay obligaciones por resolver", "Tu plan está estable, pero la deuda sigue marcando el ritmo", "Tu plan está claro y bajo control") + guidance + pressure. Note: watch conflates magnitude (spentPercent>=90) with needs-action (pendingObligations, stale freshness) — the spec splits these into cerca vs atencion. | Map to the 4-state machine: spentPercent>=100 or negative margin → te-pasaste; 75-99 → cerca; pendingObligations/freshness → atencion; else vas-bien. Headlines become verdict detail lines. | 1 | medium |
| 13 | `webapp/src/actions/plan.ts:196-203, 241-250` | plan-overlimit-thresholds | PlanBudgetSummary counts feeding chips/rails/drill-cards everywhere: `overLimitCategories = categories.filter((c) => (c.budget ?? 0) > 0 && c.percentUsed > 100)`; `nearLimitCategories = ... c.percentUsed >= 85`; attentionCategories = dedup(over+near) sorted worst-first slice 4. Returned as budget.overLimitCount / nearLimitCount / attentionCategories. | Canonical counts source for GroupSummary ("N sobre límite") and cerca threshold moves 85 → 75 to match the closed vocabulary semantics. | 1 | trivial |
| 14 | `webapp/src/components/mobile/v2/plan/plan-net-hero.tsx:50-102` | plan-mobile-hero-no-verdict | Mobile Plan hero (mounted via plan-root.tsx:58): eyebrow "Neto del mes" 10px uppercase text-z-brass (brass eyebrow) + meta "{daysRemaining} días restantes" → 3xl number colored by sign (`neto > 0 ? "text-z-income" : neto < 0 ? "text-z-expense" : "text-z-brass"`) → stacked income/expense/brass bar → legend → "Toca para ver flujo" brass expand affordance. NO verdict word anywhere — number color is the only judgment. | Adopt full Verdict slot: chip under the number (state from plan pressure/margin), detail sentence, meta row. Expand label becomes Disclosure "Ver flujo por día" (T3). | 1 | medium |
| 15 | `webapp/src/components/mobile/v2/deudas/deudas-hero.tsx:43-88` | deudas-hero-mobile-no-verdict | Mobile Deudas hero (mounted deudas-lens-root.tsx:131): eyebrow MOBILE_EYEBROW_CLASS "Cuota mensual" → 32px `font-[680] tracking-[-0.05em] tabular-nums` number + right-aligned interest (`text-z-debt` 16px + "en intereses" 10px) → capital/interés split bar (`from-z-white/95 to-z-white/70` capital, `from-z-debt/80 to-z-debt/50` interest) → meta "{capitalPct}% capital · {interestPct}% costo financiero". NO verdict. Verdict slot inserts between the hero amounts row (line 56-69) and the split bar (line 72). | Insert <Verdict /> per T6: "Vas bien · 24% del ingreso" chip (white V1 number stays uncolored when vas-bien), detail line, split bar stays as T2 evidence. | 1 | small |
| 16 | `webapp/src/components/debt/debt-hero-card.tsx:24-76` | deudas-hero-desktop-no-verdict | Desktop Deudas hero (deudas/page.tsx:208): Card with two gradient tiles — left "Deuda total" label + 2xl/3xl bold number + "Pagas al mes" + number; right "Intereses / mes" uppercase label + 2xl text-z-expense number + "Dinero que no reduce tu deuda". Everything expense-orange (`border-z-expense/15 bg-gradient-to-br from-z-expense/8 to-z-expense/4`, icon tiles `bg-z-expense/10`). No eyebrow→number→verdict structure, no judgment word. | Restructure to canonical hero slot with <Verdict /> under the deciding number (cuota vs ingreso), debt-red reserved for te-pasaste states only. | 1 | medium |
| 17 | `webapp/src/components/mobile/v2/plan/mobile-recurrentes-view.tsx:241-264` | recurrentes-hero-no-verdict | Recurrentes hero (route /recurrentes redirects to /plan?tab=recurrentes → this view on mobile): HERO_CARD_GRADIENT_CLASS card, eyebrow "Compromiso mensual" `text-[10px] font-semibold uppercase tracking-[0.18em] text-z-brass` (brass eyebrow), 3xl tabular number, then two stat chips: "Pendientes" count in text-z-alert and "Completados" count in text-z-income separated by divider. No verdict word; alert/income colors carry unlabeled judgment. | Full slot: eyebrow (sage-dark not brass) → number → <Verdict /> ("Vas bien" when all paid / atencion when payment due) → detail "N de M pagados" (T6 group grammar) → meta. Feeds T4 'Todo pagado' celebration later. | 1 | medium |
| 18 | `webapp/src/components/plan/tabs/plan-tab-recurrentes.tsx:68-99` | recurrentes-desktop-header | Desktop Recurrentes: h2 "Recurrentes" + subtitle "{activeCount} plantillas activas", SummaryCard metrics (Plantillas activas / Salidas/mes / Entradas/mes) and `<AttentionCard signals={...page === "recurrentes"} />`. No hero number, no verdict slot. | Adopt canonical hero with <Verdict /> slot; AttentionCard signals fold into the atencion state. | 1 | medium |
| 19 | `webapp/src/components/ui/attention-card.tsx:24-61` | attention-card-dialect | AttentionCard header: `{hasSignals ? "Necesita atención" : "Estado"}` as 10px uppercase eyebrow in `text-z-brass` when signals exist (brass carries the verdict), `text-z-olive-deep` otherwise; empty state renders CheckCircle2 + "Al día" in text-z-olive-deep. Used on Presupuesto desktop (plan-tab-presupuesto.tsx:302) and Recurrentes desktop (plan-tab-recurrentes.tsx:96). "Necesita atención" is the exact header the T1 table says atencion replaces. | "Necesita atención" → <Verdict state="atencion"> (word "Atención", TriangleAlert icon, --z-alert 8%/20%) — never brass; "Al día" → "Vas bien". | 1 | small |
| 20 | `webapp/src/app/(dashboard)/layout.tsx:85-92` | attention-summary-strings | attentionSummary fed to the mobile shell: `?? \`${attentionSnapshot.totalAction} pendientes requieren atención\`` / `\`${totalSuggestion} sugerencia(s) para revisar\`` / `"Todo en orden por ahora"` — a third phrasing family for the same atencion/vas-bien judgment. | Copy converges on closed vocabulary: atencion detail line grammar; "Todo en orden por ahora" → "Vas bien" family. | 1 | trivial |
| 21 | `webapp/src/components/mobile/v2/inicio/widgets/attention-widget.tsx:40-73` | attention-widget-por-resolver | Inicio "Por resolver" widget: `tone = overdueCount > 0 ? "debt" : total > 0 ? "brass" : "foreground"` (brass carries needs-action), empty state CheckCircle2 text-z-income + "Al día" 12px semibold, caption `"${overdueCount} vencido(s)"` / `"${upcomingCount} próximo(s)"` / `"Sin pendientes"`. This is the inbox the atencion state feeds. | atencion state binds to --z-alert (not brass); "Al día" re-words to vas-bien vocabulary; compact Verdict form for the chip. | 1 | small |
| 22 | `webapp/src/components/dashboard/flujo-section.tsx:61-76` | flujo-subtitle-verdict | Dashboard Flujo section subtitle generator: `flujoSubtitle = \`Gastaste ${X} de ${Y} — por encima del ingreso\`` when expenses > income, `\`... — vas bien\`` when income > 0, else `\`${X} en gastos este mes\`` / "Importa tu extracto para ver tu flujo". Lowercase "vas bien" inside a subtitle — a second full-volume verdict on the dashboard screen. | Section subtitles stop carrying verdict words (one full-volume verdict per screen); keep as neutral evidence or delete. | 1 | trivial |
| 23 | `webapp/src/components/dashboard/presupuesto-section.tsx:45-58` | presupuesto-subtitle-verdict | Dashboard Presupuesto section subtitle: totalSpentPct > 100 → `\`Necesidades al ${needsPct}% — por encima del presupuesto\``; >= 90 → `\`... — cerca del limite\`` (unaccented); else `\`... — dentro del presupuesto\``. Same threshold family as StatusHeadline (100/90) — a third dashboard verdict voice. | Delete verdict clause from subtitle (verdict lives once in the hero); fix accent if any copy survives. | 1 | trivial |
| 24 | `webapp/src/components/plan/plan-decision-rail.tsx:82-93` | plan-rail-and-section-strings | Plan desktop decision rail budget cell: `budget.overLimitCount > 0 ? \`${n} en alerta\` : budget.nearLimitCount > 0 ? \`${n} cerca del límite\` : "Bajo control"`. Sibling plan-budget-section.tsx:69-75 says `\`${n} categorías ya van por encima\`` / `\`${n} categorías están cerca del límite\`` / "Tus categorías no muestran alertas fuertes" — two more synonym families ("en alerta", "ya van por encima", "Bajo control"). | Group-summary grammar: "N sobre límite" / "N en orden" per copy rule 6; verdict words only from the closed set. | 1 | small |
| 25 | `webapp/src/components/mobile/v2/plan/plan-zone-chips.tsx:133-146, 174-180` | plan-zone-chips-overlimit | Plan mobile expanded Presupuesto panel: DetailRow `label="Sobre el límite" value={\`${overLimitCount} categoría(s)\`} valueClass="text-z-debt"`; "Sin categorizar" uses `valueClass="text-amber-400"` (hardcoded Tailwind palette color, not a z-token); Obligaciones panel "Próximos a vencer" also `text-amber-400`. | "Sobre el límite" → group-summary "N sobre límite"; amber-400 → --z-alert token. | 1 | trivial |
| 26 | `webapp/src/components/mobile/v2/plan/plan-drill-cards.tsx:43-59` | plan-drill-cards-brass-verdict | Plan drill cards captions: `overLimit > 0 ? <span className="font-semibold text-z-brass">{overLimit} sobre límite</span> : "dentro del límite"`; Periodo caption `percentAssigned >= 100 ? "al día" : <span className="font-semibold text-z-brass">{unassigned} pendientes</span>`; Recurrentes `<span className="font-semibold text-z-brass">{overdueCount} vencidas</span>`. Over-limit and overdue judgments rendered in BRASS. | Status words re-bind to semantic tokens (debt/alert), brass reserved for actions; captions adopt "N sobre límite" compact verdict form. | 1 | small |
| 27 | `webapp/src/components/dashboard/burn-rate-card.tsx:36-46` | burn-rate-trend-labels | Desktop Ritmo de gasto card (live via flujo-section dynamic import): `trendLabel = { accelerating: "↑ Acelerando", stable: "→ Estable", decelerating: "↓ Desacelerando" }` with `trendColor = { accelerating: "text-red-400", stable: "text-zinc-400", decelerating: "text-green-400" }` — hardcoded Tailwind palette colors (not z-tokens) rendering a status judgment next to the 3xl runway number; also `text-destructive` at line 117. | Trend stays as chart evidence (T2 explains, never judges); colors re-bind to z-tokens; any judgment moves to the screen's single Verdict. | 3 | small |
| 28 | `webapp/src/components/tendencias/verdict-header.tsx:6-15` | tendencias-verdict-header-brass | Tendencias already has a `VerdictHeader` (mounted tendencias-shell.tsx:33): brass callout `flex items-center gap-3 rounded-2xl border border-z-brass/25 bg-z-brass/8 p-3` + `<TrendingUp className="size-4 shrink-0 text-z-brass" />` + verdict.headline. Headline generated in packages/shared/src/analytics/verdict.ts:17-40 (`"Tu tasa de ahorro es ${ratePct}%${dir}."`, fallback "Aún no hay suficiente historial para un veredicto."). BRASS carries the verdict — direct guardrail violation. | Re-bind to <Verdict /> chip + detail line with semantic tokens; brass removed from the verdict surface. | 1 | small |
| 29 | `webapp/src/components/budget/scenario/scenario-verdict.tsx:127-139` | scenario-verdict-pills | Simulador verdict pill: `fits ? "border-z-income/35 bg-z-income/8 text-z-income" : "border-z-debt/35 bg-z-debt/8 text-z-debt"` with copy `\`sobran ${X}\`` / `\`faltan ${X}\`` + "/mes" at `text-[10px] font-bold`. Sibling scenario-commit.tsx:110-117 repeats it via StateChip (`sobran .../mes` sage \| `faltan .../mes` danger); scenario-shared.tsx:30 has a 9px uppercase z-alert pill. Recipe is already ~8%/20-35% tint — closest existing relative of the Verdict chip. | Adopt <Verdict /> with delta prop (`state · delta` grammar) instead of bespoke pills. | 1 | small |
| 30 | `webapp/src/components/mobile/v2/deudas/debt-trend-card.tsx:13-17, 118` | debt-trend-statechip | Deudas mobile trend card STATUS_META: `{ mejorando: { label: "Mejorando", variant: "sage" }, estable: { label: "Estable", variant: "brass" }, mes_pesado: { label: "Mes pesado", variant: "danger" } }` rendered as uppercase StateChip — three more off-vocabulary verdict words, one in brass. | Compact Verdict form or plain meta text; "Mes pesado"/"Mejorando" leave the verdict register (chart explains, hero judges). | 1 | small |
| 31 | `webapp/src/components/mobile/v2/plan/plan-budget-hero.tsx:23-27, 100-114, 128-158` | budget-hero-dead-code | PlanBudgetHero (labels `{ stable: "Holgado", watch: "Atención", critical: "Crítico" }` + StateChip + per-category `>= 100 ? "text-z-debt" : >= 80 ? "text-z-alert" : "text-z-income"` rows + footer "X pts sobre/bajo ritmo") is DEAD CODE: its only consumer plan-hero-wrapper.tsx has zero importers. Same for webapp/src/components/mobile/mobile-dashboard-v2.tsx (no importers) and its children mobile-spending-pace.tsx (lines 26-30 "No llegas al cierre"/"Ajusta para cerrar"/"Ritmo bajo control", line 118 "Vas bien — llegas al día X con margen."), mobile-budget-ring.tsx (lines 37-41 "Sobre el límite"/"Último tramo"/"Con margen"), dashboard/burndown-expandable.tsx (lines 40-50 same triad, line 133 "Vas bien — llegas ... con margen."), and mobile/v2/inicio/pulse-widget.tsx (lines 59-70 "En ritmo"/"Arriba del ritmo" in brass — removed from InicioRoot 2026-05-21 per comment at inicio-root.tsx:333). | Delete during Fase 1 cleanup — these carry 8+ off-vocabulary verdict strings that would otherwise be migrated for nothing. | 1 | small |
| 32 | `webapp/src/app/globals.css:243-247` | verdict-chip-recipe-exists | The exact Verdict chip color recipe already exists as utilities: `.surface-income { color: var(--z-income); border-color: color-mix(in srgb, var(--z-income) 20%, transparent); background: color-mix(in srgb, var(--z-income) 8%, transparent); }` plus .surface-expense / .surface-debt / .surface-alert / .surface-neutral. The T1 spec explicitly says 'existing .surface-* recipe'. | <Verdict /> binds its 4 states to .surface-income (vas-bien), .surface-alert (cerca + atencion), .surface-debt (te-pasaste) — zero new styles needed. | 1 | trivial |
| 33 | `webapp/src/components/marketing/landing-demo.tsx:76-91` | marketing-vas-bien | Landing demo verdict strings (marketing, outside app shell): `{ text: "Vas bien.", color: "#5CB88A", glow: "rgba(61,158,110,0.6)", state: "good" }` and metaText `\`+ $ ${x}k sobre el promedio · quedan ${left} días\`` — hardcoded hex colors. Shares vocabulary but not tokens. | Out of T1 migration scope (marketing surface); note only so vocabulary stays aligned if touched. | 1 | trivial |
| 34 | `webapp/src/actions/health-meters.ts:77-100` | health-copy-generators | Health meter sentence generators: `\`Tu ${worstLabel} está en zona crítica y necesita atención inmediata.\``, `\`Múltiples áreas en rojo — ... requieren atención urgente.\``, `\`En general vas bien — solo tu ${worstLabel} merece atención antes de que suba de nivel.\``; plus lib/health-levels.ts:288 `"... Vas bien."`. Verdict-adjacent tuteo copy consumed by savings-rate widget in dashboard presupuesto-section. | Copy survives as detail-line material but drops standalone verdict claims ("vas bien") — the word belongs to the chip. | 1 | small |
| 35 | `webapp/src/components/dashboard/hybrid-hero.tsx:207-220` | hybrid-hero-progress-gradient | Inicio hero period progress bar paints a 4-stop severity gradient regardless of state: `background: "linear-gradient(to right, var(--color-z-excellent) 0%, var(--color-z-sage) 30%, var(--color-z-alert) 70%, var(--color-z-debt) 100%)"` — the bar itself renders all verdict hues simultaneously. | Bar becomes single-hue evidence (T2/T6: healthy = white/25 or brass-for-gauge only, debt only at >=100); severity expressed once by the Verdict chip. | 3 | trivial |

### headers-month — 30 findings

| # | File | Topic | Current | Target | Phase | Effort |
|---|---|---|---|---|---|---|
| 1 | `webapp/src/app/(dashboard)/dashboard/page.tsx:185-199` | header-dashboard-desktop | Desktop Inicio header: eyebrow <p class="text-xs font-semibold uppercase tracking-[0.18em] text-z-sage-dark">Inicio</p>, <h1 class="text-3xl font-semibold tracking-tight">Tu estado financiero de hoy</h1>, subtitle <p class="text-muted-foreground">{monthLabel} · claridad para decidir sin perderte entre métricas</p> (monthLabel lowercase e.g. "julio 2026"), <MonthSelector /> top-right inside Suspense. | Canonical header: H1 28px/700 "Inicio" (matches tab name exactly), no eyebrow-as-title-prefix, no subtitle, <MonthControl /> top-right with "Julio 2026" label. | Fase 2 · Marco (T5) | small |
| 2 | `webapp/src/app/(dashboard)/dashboard/page.tsx:141-156` | header-dashboard-starter | Starter-mode branch renders <h1 class="text-2xl font-bold">Inicio</h1> + subtitle <p class="text-muted-foreground">Tu base ya está lista. Falta activar tu flujo.</p> — a second header dialect on the same route, no month control. | Same canonical header row (title left, MonthControl right); subtitle removed per no-subtitles rule. | Fase 2 · Marco (T5) | trivial |
| 3 | `webapp/src/components/dashboard/zones/mobile-zone.tsx:147` | header-dashboard-mobile-wordmark | Mobile Inicio header is <MobileHeader variant="main" title="Zeta" /> — wordmark-as-header. NO month control exists anywhere in the mobile dashboard branch (month param is consumed by data fetches but the user cannot step months on mobile Inicio). | Title "Inicio" (tab name), wordmark leaves headers (splash/auth only); <MonthControl /> in the header row so mobile Inicio joins the shared clock. | Fase 2 · Marco (T5) | small |
| 4 | `webapp/src/components/mobile/v2/mobile-header.tsx:43-103` | mobileheader-component-grammar | MobileHeader main variant: title <p class="truncate text-[15px] font-bold leading-tight text-foreground"> + optional subtitle <p class="truncate text-[11px] text-muted-foreground">; action slot right + MobileAvatarMenu. Supports subtitles (used by Plan/Deudas). No month-control slot. | H1 22px/600 sentence case, no subtitles; month control occupies the right slot (chevron · "Abril 2026" · chevron in compact-card container). | Fase 2 · Marco (T5) | medium |
| 5 | `webapp/src/app/(dashboard)/transactions/page.tsx:133-153` | header-movimientos-desktop | Desktop uses <PageHero> with pills row: <HeroPill>Movimientos</HeroPill>, conditional <HeroAccentPill>{activeFilterCount} filtros activos</HeroAccentPill>, <HeroPill>{monthLabel}</HeroPill> (HeroPill css uppercases it → "JULIO 2026"), title="Movimientos" (h1 text-2xl lg:text-3xl in page-hero.tsx:43), long description paragraph built at lines 105-108 (`${monthLabel} en una sola vista: ingresos, gastos y excepciones…`), MonthSelector + TransactionFormDialog in actions slot. | Month pill above title deleted; title left "Movimientos", MonthControl right, no description subtitle. Month name only in the control ("Julio 2026"). | Fase 2 · Marco (T5) | medium |
| 6 | `webapp/src/components/mobile/v2/movimientos/movimientos-root.tsx:163-173` | header-movimientos-mobile | <MobileHeader variant="main" title="Movimientos" /> then a SEPARATE centered row below: <div className="flex justify-center"><Suspense fallback="..."><MonthSelector /></Suspense></div> — the below-hero selector pattern the handoff deletes. | Selector moves into the header row (title left, MonthControl right); centered below-header selector deleted. | Fase 2 · Marco (T5) | small |
| 7 | `webapp/src/app/(dashboard)/plan/page.tsx:155-175` | header-plan-desktop | Desktop Plan header: <SectionEyebrow>Plan</SectionEyebrow>, <h1 class="text-3xl font-semibold tracking-tight">Tu capa estratégica</h1>, subtitle <p class="text-muted-foreground">{monthLabel} · reúne presupuesto, deuda, obligaciones y escenarios en una sola decisión</p>, right side PlanTabNav + conditional <MonthSelector /> (showMonthSelector = activeTab !== "presupuesto" \|\| hasSavedBudget, line 60). Selector also shows on non-month-scoped tabs (periodo, deseos). | Title "Plan" (tab name), no subtitle, MonthControl right. Month name leaves the subtitle prose. | Fase 2 · Marco (T5) | small |
| 8 | `webapp/src/components/mobile/v2/plan/plan-root.tsx:44-55` | header-plan-mobile-resumen | <MobileHeader variant="main" title="Plan" subtitle={`${monthLabel} · ${daysInMonth - dayOfMonth}d restantes`} /> plus a centered <MonthSelector /> below the header (flex justify-center, Suspense fallback shows lowercase monthLabel). | Title "Plan", subtitle deleted, MonthControl in header right slot; below-hero selector removed. | Fase 2 · Marco (T5) | small |
| 9 | `webapp/src/app/(dashboard)/plan/page.tsx:134-150` | header-plan-mobile-subtabs-abr-pill | Non-resumen tabs on mobile render <MobileHeader variant="main" title={MOBILE_TAB_TITLES[activeTab]} /> (Presupuesto/Periodo/Recurrentes/Deseos) + centered <MonthSelector compact /> whose label is formatMonthLabelShort → "Jul" (3-letter, capitalized by CSS `capitalize`, year-less). This is the live "Abr" pill the handoff deletes. | MonthControl with full "Julio 2026" label in the header row; short year-less month label banned outside chart axes. | Fase 2 · Marco (T5) | small |
| 10 | `webapp/src/components/plan/tabs/plan-tab-presupuesto.tsx:177-183` | header-presupuesto-duplicate-mobile | PlanTabPresupuesto's mobile branch renders its OWN <MobileHeader variant="sub" title="Presupuesto" backHref="/plan" action={<BudgetAjustesSheet variant="icon" …/>} /> — while plan/page.tsx:136 already rendered <MobileHeader variant="main" title="Presupuesto"> for the same view → two sticky headers mount for ?tab=presupuesto on mobile. | One canonical header per screen; duplicate removed when converging on MonthControl header grammar. | Fase 2 · Marco (T5) | small |
| 11 | `webapp/src/components/plan/tabs/plan-tab-presupuesto.tsx:269-289` | header-presupuesto-desktop | Desktop Presupuesto header inside Plan: <h2 class="text-2xl font-semibold">Presupuesto</h2> + subtitle <p class="text-sm text-muted-foreground">{monthLabel} · {daysRemaining} días restantes</p> (lowercase "julio 2026"); actions: "Armar presupuesto" link, BudgetAjustesSheet, MonthPlanner. monthLabel repeats at line 297 (SummaryCard context) and line 310 (BudgetSummaryBar prop). | Title-left/control-right grammar; month name only in the MonthControl, not in subtitle prose. | Fase 2 · Marco (T5) | small |
| 12 | `webapp/src/components/plan/tabs/plan-tab-recurrentes.tsx:50-66` | header-recurrentes-duplicate-mobile | Mobile branch renders <MobileHeader variant="sub" title="Recurrentes" backHref="/plan" /> (line 51) while plan/page.tsx:136 already rendered <MobileHeader variant="main" title="Recurrentes"> — second duplicated header. Desktop header at lines 70-76: <h2 class="text-2xl font-semibold">Recurrentes</h2> + subtitle "{summary.activeCount} plantillas activas". | Single canonical "Recurrentes" header with MonthControl; subtitle count moves to a GroupSummary (T6), not the header. | Fase 2 · Marco (T5) | small |
| 13 | `webapp/src/app/(dashboard)/deudas/page.tsx:289-299` | header-deudas-mobile | Mobile: <MobileHeader variant="main" title="Deudas" subtitle={`Lectura en ${currency}`} /> + centered <MonthSelector /> below (flex justify-center, Suspense skeleton h-9 w-40). | Title "Deudas", no subtitle, MonthControl in header right slot. | Fase 2 · Marco (T5) | small |
| 14 | `webapp/src/app/(dashboard)/deudas/page.tsx:314-334` | header-deudas-desktop | Desktop: <PageHeaderRow title="Deudas" subtitle={`Lectura en ${currency}`} actions={…"Planificador de pagos" brass Button, "Volver a Plan" ghost, <MonthSelector />} />. PageHeaderRow (webapp/src/components/ui/page-header-row.tsx:32-37) renders h1 text-[22px] font-bold + mt-0.5 text-sm text-muted-foreground subtitle. | PageHeaderRow is the closest existing shape (title left / actions right, 22px) — converges to H1 22/600 (28/700 desktop), subtitle dropped, MonthControl replaces MonthSelector. | Fase 2 · Marco (T5) | small |
| 15 | `webapp/src/components/tendencias/tendencias-shell.tsx:25-35` | header-tendencias | Single shell for mobile+desktop: <SectionEyebrow>Análisis</SectionEyebrow> + <h1 class="text-2xl font-semibold tracking-tight lg:text-3xl">Tendencias</h1>, <ExportButton> top-right. No MobileHeader at all on /tendencias (no sticky app header on mobile). Month/range control is a chip row BELOW the verdict: <PeriodControl range={vm.range} /> (line 35). | Canonical header with the RANGE variant of MonthControl top-right: "Nov 2025 – Abr 2026" fixed-width (min 120px), chevrons slide the 6-month window; eyebrow-title stack replaced by plain title. | Fase 2 · Marco (T5) | medium |
| 16 | `webapp/src/app/(dashboard)/dashboard/page.tsx:100-103` | month-state-url-param-per-page | Month IS already URL state (?month=YYYY-MM) but parsed independently per page: dashboard/page.tsx:100-103 (parseMonth + formatMonthLabel), transactions/page.tsx:63-65, plan/page.tsx:40+46, deudas/page.tsx:280. Current month = param ABSENT (MonthSelector deletes the key when landing on today's month). No context/provider; the "store" is searchParams re-read per surface. | ONE app-level month key in the URL shared by all five surfaces — same key name, same parse helper, one <MonthControl /> writing it. | Fase 2 · Marco (T5) | medium |
| 17 | `webapp/src/components/month-selector.tsx:16-76` | month-stepper-monthselector | Primary stepper. Three shadcn Buttons in `flex items-center gap-1`: ChevronLeft h-4 w-4 (variant outline size icon-sm, aria-label "Mes anterior"), center Button (variant secondary when current, min-w-[120px] sm:min-w-[160px] capitalize) showing formatMonthLabel → "Julio 2026"; clicking the label JUMPS to current month. ChevronRight disabled={isCurrent} (aria-label "Mes siguiente"). compact prop → formatMonthLabelShort "Jul" with min-w-0 px-2 text-xs capitalize. navigateToMonth: URLSearchParams, deletes month at current, resets page to 1, router.replace (line 40, comment documents back-button rationale). | <MonthControl />: chevron · "Abril 2026" · chevron inside ONE compact-card container (#111, border white/6, radius 10), chevrons 14px stroke 1.5 hit 32×44, fixed-width tabular label min 88px, next disabled at current (rgba(255,255,255,0.15)). Label-click-jumps-to-today and the compact "Jul" form are deleted. | Fase 2 · Marco (T5) | medium |
| 18 | `webapp/src/components/recurring/use-recurring-month.ts:122-157` | month-cursor-recurring-duplicate | SECOND month-cursor implementation: reads searchParams.get("month") via parseMonth, exposes goNextMonth/goPrevMonth/navigateToMonth (router.replace, deletes param at current month — lines 134-148), builds its own monthLabel = format(monthCursor, "MMMM yyyy", { locale: es }) (line 132, lowercase). Consumed by RecurringTimelineView (desktop) and MobileRecurrentesView. | Deleted in favor of the single MonthControl + shared month store; occurrence loading keys off the shared month value. | Fase 2 · Marco (T5) | medium |
| 19 | `webapp/src/components/recurring/recurring-mini-calendar.tsx:58-80` | month-stepper-recurring-mini-calendar | Desktop Recurrentes calendar has its own month nav header: ghost Button size icon-xs ChevronLeft/ChevronRight (aria-labels "Mes anterior"/"Mes siguiente") around <span class="text-sm font-medium capitalize">{monthLabel}</span>. NO future clamp — next chevron is never disabled (onNavigate(1) unconditional). Wired via recurring-timeline-view.tsx:32-36 handleNavigate delta adapter. | Calendar keeps its grid but month stepping defers to the page-level MonthControl (one clock); if an inline nav stays it must disable next at current month per spec. | Fase 2 · Marco (T5) | small |
| 20 | `webapp/src/components/recurring/recurring-hero-compact.tsx:34-54` | month-stepper-orphaned-recurring-hero | ORPHANED (no importers found): RecurringHeroCompact renders a third stepper — size-7 rounded-full bordered chevron buttons + <span class="text-xs font-medium capitalize text-muted-foreground">{monthLabel}</span>, disabled={!canGoNext}. Its only would-be consumer mobile-recurring-manager.tsx:98-104 passes canGoNext={true} hardcoded and is itself unimported (dead code). | Delete both files during the T5 sweep (dead month-stepper dialect). | Fase 2 · Marco (T5) | trivial |
| 21 | `webapp/src/components/tendencias/period-control.tsx:5-41` | range-state-tendencias | Tendencias time state is a SEPARATE URL key ?range= with values WTD\|MTD\|3M\|6M\|12M\|YTD (labels "Semana", "Mes", "3M", "6M", "12M", "Año"), rendered as chip buttons (ACTIVE_CHIP brass tint) below the verdict; setRange uses router.replace (line 26). page.tsx:37 defaults to "6M". No chevron window-sliding; no month anchor shared with other surfaces. | Range variant of MonthControl: chevron · "Nov 2025 – Abr 2026" · chevron sliding a 6-month window, top-right in the header, sharing the app month key as anchor. | Fase 2 · Marco (T5) | large |
| 22 | `webapp/src/components/mobile/v2/mobile-tab-bar.tsx:18-39` | month-reset-on-tab-switch | Tab bar links use bare static hrefs (tab.href from lib/constants/mobile-nav.ts:21-47 — "/dashboard", "/transactions", "/plan", "/deudas", "/gestionar") so switching tabs DROPS ?month=. Desktop sidebar identical: nav-item-link.tsx:46 href={item.href} with static PRIMARY_NAV hrefs (lib/constants/navigation.ts:32-43). Violates "Tab switches never reset it; stepping on Plan steps Movimientos". | Nav links carry (or the app store rehydrates) the shared month key so the clock survives surface switches. | Fase 2 · Marco (T5) | medium |
| 23 | `webapp/src/components/plan/plan-tab-nav.tsx:17-24` | month-preserved-plan-tabs-only | Only place month survives navigation: PlanTabNav.buildHref sets params.set("month", month) when switching Plan tabs (desktop). Mobile equivalent PlanDrillCards (webapp/src/components/mobile/v2/plan/plan-drill-cards.tsx:84-102) uses static hrefs "/plan?tab=presupuesto" etc. and DROPS the month. | Uniform: all intra/inter-surface nav preserves the single month key. | Fase 2 · Marco (T5) | trivial |
| 24 | `webapp/src/lib/utils/date.ts:95-131` | format-helpers-date-ts | parseMonth (YYYY-MM regex, falls back to current month), formatMonthParam ("yyyy-MM"), formatMonthLabel → format(date, "MMMM yyyy", {locale: es}) returns LOWERCASE "julio 2026" (capitalization only via CSS `capitalize` in MonthSelector), formatMonthLabelShort → "MMM" no year ("jul"). Also formatDate default pattern "dd MMM yyyy" (line 12) → rows read "15 abr 2026", not the ladder's "15 de abril". | Label helper produces "Abril 2026" (capitalized, tabular-nums); short form restricted to chart axes ("abr" lowercase, no period); row format "15 de abril" with "de 2025" only when year differs. | Fase 2 · Marco (T5) | small |
| 25 | `webapp/src/components/tendencias/anomalies-card.tsx:24` | short-month-outside-axes | monthShort (tendencias/format.ts:1-7, MESES array "ene"…"dic") used in PROSE: anomalies-card.tsx:24 `{formatCurrency(a.amount, currency)} en {monthShort(a.month)} — {a.multiple.toFixed(1)}× tu promedio.` and forecast-card.tsx:47 `Proy. {monthShort(last.month)}` caption. Also "MMM yyyy" outside axes: dashboard/debt-free-banner.tsx:13, accounts/statement-history-timeline.tsx:273, accounts/statement-snapshots-card.tsx:32 ("Último: {formatDate(lastPeriod, \"MMM yyyy\")}"). | 3-letter months only on chart axes; prose/details use "15 de abril" / "Abril 2026" ladder rungs. | Fase 2 · Marco (T5) | small |
| 26 | `webapp/src/app/(dashboard)/transactions/page.tsx:105-108` | month-label-in-prose-subtitles | monthLabel woven into sentence copy across surfaces: transactions description `${monthLabel} en una sola vista: …` + scopeLabel = monthLabel.toLowerCase() (line 105); dashboard subtitle page.tsx:193; plan subtitle plan/page.tsx:163; plan-teaser-card.tsx:50 `{monthLabel} reúne presupuesto…`; plan-tab-presupuesto.tsx:273/297; movimientos-root.tsx date-group eyebrow formatDate(date, "EEEE, dd MMM") (line 219). | "Abril 2026" appears ONLY in the header control; subtitles deleted; row/group dates follow the ladder ("15 de abril", relative only in Actividad reciente). | Fase 2 · Marco (T5) | medium |
| 27 | `webapp/src/app/(dashboard)/deudas/page.tsx:49-50` | past-month-no-readonly | NO "Mes cerrado" / read-only concept exists anywhere (grep for "Mes cerrado"/"solo lectura" returns nothing). Past-month handling is data-only: deudas/page.tsx:49-50+69 computes isCurrentMonth = !month \|\| month >= toColombiaDateString(new Date()).slice(0,7) and skips getDebtTrend for past months; plan-timeline.ts:155-206 treats past months as fully-elapsed. Capture actions (FAB, QuickCaptureBar, TransactionFormDialog) stay visible on past months; verdict copy has no past tense. | Past months read-only: lock icon 11px + "Mes cerrado · solo lectura" (11px sage-dark) under header, capture actions hidden, verdicts in past tense ("Cerraste bajo plan"). | Fase 2 · Marco (T5) | large |
| 28 | `webapp/src/components/month-selector.tsx:37-40` | router-replace-discipline-ok | All three month/range navigations already use router.replace, never push: month-selector.tsx:40 (with explanatory comment), use-recurring-month.ts:142-145 (same comment), period-control.tsx:26. Back-button discipline for the month cursor is ALREADY compliant with the handoff. | Keep replace semantics when consolidating into MonthControl — no change needed, just don't regress. | Fase 2 · Marco (T5) | trivial |
| 29 | `webapp/src/components/recurring/recurring-timeline-view.tsx:32-36` | recurrentes-no-future-clamp | Recurrentes surfaces allow stepping into FUTURE months (mini-calendar next chevron never disabled; orphaned manager passes canGoNext={true}), while MonthSelector disables next at current month — two conflicting clamp policies in the app today. | Spec says next chevron disabled at current month everywhere; if Recurrentes needs future preview it becomes an explicit product exception, not a component fork. | Fase 2 · Marco (T5) | small |
| 30 | `webapp/src/app/(dashboard)/recurrentes/page.tsx:3-5` | route-aliases-redirect | /recurrentes redirects to /plan?tab=recurrentes and /presupuesto redirects to /plan?tab=presupuesto (presupuesto/page.tsx:3-5) — both drop any ?month= on the incoming URL. The Recurrentes and Presupuesto "surfaces" physically live inside plan/page.tsx tab switch (lines 76-89). | Redirects forward the shared month key (or the surfaces adopt the canonical header where they actually render, inside Plan tabs). | Fase 2 · Marco (T5) | trivial |

### disclosure-motion — 61 findings

| # | File | Topic | Current | Target | Phase | Effort |
|---|---|---|---|---|---|---|
| 1 | `webapp/src/app/globals.css:70-198` | motion-tokens-home | No --motion-* tokens exist. The :root token block (lines 70-198) already hosts --z-space-*, --z-mobile-*, and the z-index scale --z-layer-raised/sticky/nav/modal/popover/toast/tooltip/dev (lines 152-159). @theme inline block is lines 7-68. tw-animate-css imported at line 2. | Add --motion-ease: cubic-bezier(0.2,0,0,1); --motion-1: 120ms; --motion-2: 160ms; --motion-3: 200ms in this :root block, next to the --z-layer-* scale. | 2 | trivial |
| 2 | `webapp/src/app/globals.css:256-262` | page-enter-keyframe | @keyframes page-enter { from { opacity: 0; transform: translateY(3px); } to { opacity: 1; transform: translateY(0); } } .animate-page-enter { animation: page-enter 0.1s ease-out; } — 100ms, +3px direction, ease-out. | Align to content-enter spec: 120ms (--motion-1), opacity + translateY(-3px)->0, --motion-ease single curve. | 2 | trivial |
| 3 | `webapp/src/components/mobile/v2/expand.tsx:7-27` | shared-expand-component | Shared <Expand> — the only grid-rows utility: "grid transition-[grid-template-rows] duration-200 ease-out" + open ? "grid-rows-[1fr]" : "grid-rows-[0fr]", inner "min-h-0 overflow-hidden". Content stays mounted during close (AnimatedAccordion rule). Consumers: deudas-cuentas-lens, debt-trend-card, deudas-plan-lens, category-trend-list, top-recipients-card. | Becomes the container of <Disclosure />: grid-rows 0fr->1fr @160ms (--motion-2, --motion-ease); grow API with label "Ver + qué"/"Ocultar …" + 12px caret + 120ms content-enter. | 2 | medium |
| 4 | `webapp/src/components/mobile/v2/header-chevron.tsx:5-15` | shared-header-chevron | <HeaderChevron>: ChevronDown "size-4 shrink-0 text-muted-foreground transition-transform duration-200" + rotate-180 when open. size-4 = 16px, default lucide stroke 2. | Disclosure caret: chevron-down 12px stroke 1.5, right of label, rotates 180 deg at --motion-2 with --motion-ease (the ONLY rotating element). | 2 | trivial |
| 5 | `webapp/src/components/mobile/cards/expandable-card.tsx:39-53` | shared-expandable-card | Second shared expander <ExpandableCard>: inline style gridTemplateRows expanded?"1fr":"0fr" with "grid transition-[grid-template-rows] duration-200 ease-out"; content "transition-opacity duration-150" + "opacity-100 delay-75". No caret/label built in (affordance left to caller's compact slot). | Fold into <Disclosure /> (160ms container, 120ms opacity+translateY(-3px) content, mandatory label+caret). | 2 | small |
| 6 | `webapp/src/components/mobile/v2/use-expandable-zone.ts:9-28` | accordion-hook | useExpandableZone — one-zone-at-a-time accordion state hook (activeZone/toggle/close/isActive). Pure state, no motion. | Keep; pairs with <Disclosure /> for zone accordions. | 2 | trivial |
| 7 | `webapp/src/components/mobile/v2/plan/plan-net-hero.tsx:32-48, 97-111` | hero-page-dim-delete | THE page-dim-on-expand the handoff deletes (only instance in webapp): const { overlayVisible, handleOverlayClick } = useChartFocusMode(expanded); {overlayVisible && <div className="fixed inset-0 z-40 bg-black/40" onClick=... />}; hero card raises itself with "relative z-50" + "transition-all". Toggle copy: "Toca para ver flujo" + ChevronDown / "Ocultar flujo" + ChevronUp (icon swap, not rotation), text-z-brass, expanded content instant-mounted {expanded && ...}. | Remove dim entirely (dim = focus leaves page only); convert to <Disclosure /> "Ver flujo del mes"/"Ocultar flujo del mes", sage-light label, rotating caret, 160ms. | 2 | small |
| 8 | `webapp/src/hooks/use-chart-focus-mode.ts:1-28` | hero-dim-hook-delete | useChartFocusMode: sets document.body.style.overflow="hidden", 50ms setTimeout before overlayVisible=true. Sole consumer is plan-net-hero.tsx. | Delete file with the dim removal. | 2 | trivial |
| 9 | `webapp/src/components/mobile/v2/inicio/inicio-burndown.tsx:252-277` | inicio-hero-silent-grower | Tappable chart button aria-label="Expandir detalle del gráfico" (banned word "Expandir"), NO visible caret or label; expander: "grid transition-[grid-template-rows] duration-200 ease-out" + gridTemplateRows inline, content "transition-opacity duration-150" + "opacity-100 delay-75". | T3 applies: Inicio hero gets Disclosure "Ver flujo por día"/"Ocultar flujo por día" with caret; 160/120ms tokens. | 2 | small |
| 10 | `webapp/src/components/mobile/v2/inicio/pulse-widget.tsx:174-227` | pulse-widget-silent-grower | Whole card is a <button> aria-label="Expandir desglose del disponible diario" (banned "Expandir"), no caret/label; grid duration-200 ease-out + "transition-opacity duration-150" delay-75. NOTE: inicio-root.tsx:333 says the PulseWidget hero was removed 2026-05-21 — no live consumer found; likely dead code. | Delete if confirmed dead; otherwise migrate to Disclosure "Ver desglose". | 2 | small |
| 11 | `webapp/src/components/dashboard/burndown-expandable.tsx:55-96` | dashboard-burndown-expandable | Header button with ChevronDown size-4 "transition-transform" + rotate-180 but NO text label (silent-ish); container "grid transition-[grid-template-rows] duration-200 ease-out" + inline gridTemplateRows; content "mt-3 space-y-3 transition-opacity duration-150" + "opacity-100 delay-75". | Disclosure with explicit label ("Ver quema de gasto"/"Ocultar …"), 160/120ms. | 2 | small |
| 12 | `webapp/src/components/dashboard/hybrid-hero.tsx:217, 243-257, 531` | hybrid-hero-toggle | Toggle label "Ver detalle"/"Ocultar detalle" (line 247) with ChevronDown "size-3.5 text-z-brass transition-transform" rotate-180 (brass caret — banned); detail mounts INSTANTLY via {expanded && (...)} — no animated container. Bar fill inline transition: "width 0.5s ease" (line 217). Heatmap cells "transition-transform active:scale-95" (line 531). | Disclosure with "Ver + qué" copy, sage-light label, 160ms grid container; kill 500ms width transition (cap 200ms or none); remove press-scale (hover-tint only). | 2 | medium |
| 13 | `webapp/src/components/mobile/v2/deudas/deudas-hero.tsx:44-99` | deudas-hero-silent-grower | Whole card <button> aria-label="Expandir desglose de cuota mensual" (banned "Expandir"), no caret/label; grid duration-200 ease-out + opacity duration-150 delay-75. | Disclosure "Ver desglose"/"Ocultar desglose" (T3 Deudas rows: "Ver desglose", split bar inside). | 2 | small |
| 14 | `webapp/src/components/mobile/v2/deudas/deudas-salary-bar.tsx:21-64` | deudas-salary-bar-silent-grower | Whole panel <button onClick={() => toggle("salary")}> with only "Tu salario" + "% libre" text — no caret, no Ver-label, no aria-label; grid duration-200 + opacity 150 delay-75; bar segments use "h-full transition-all". | Disclosure with caret + "Ver distribución"/"Ocultar distribución". | 2 | small |
| 15 | `webapp/src/components/mobile/v2/deudas/deudas-cuentas-lens.tsx:141-184, 245-310, 355-457, 555-671` | deudas-cuentas-lens-expanders | Four <Expand>+<HeaderChevron> expanders (lines 156, 270, 381, 583): ClosedObligations, personal debts summary, Deuda total tiles, per-account rows. Carets present, but headers carry no "Ver + qué" label; inner rows use "active:opacity-80" (line 162). | Migrate to <Disclosure /> keeping card-header pattern; add labels per T3 Deudas application ("Ver desglose"); tokens 160/120ms. | 2 | medium |
| 16 | `webapp/src/components/mobile/v2/deudas/debt-trend-card.tsx:108-132, 229-364` | debt-trend-card-expander | Clickable header div (cursor-pointer) + nested chevron <button> aria-label "Ver detalle de tendencia"/"Ocultar detalle de tendencia" with <HeaderChevron>; two nested <Expand> (lines 229, 313); bars use "transition-opacity duration-150" (line 182), month buttons "transition-colors duration-150" (line 242). | Disclosure; aria copy already compliant — surface it as the visible label. | 2 | small |
| 17 | `webapp/src/components/mobile/v2/deudas/deudas-plan-lens.tsx:215-307` | deudas-plan-lens-expanders | Two <Expand> blocks (lines 225, 307) triggered by header buttons with {hasDetail && <HeaderChevron open={open} />} — caret only, no text label. | Disclosure with "Ver + qué" labels. | 2 | small |
| 18 | `webapp/src/components/mobile/v2/inicio/inicio-activity.tsx:228-246` | inicio-activity-row-expand | Activity rows expand TransactionQuickActions: "grid transition-[grid-template-rows] duration-200 ease-out" + gridTemplateRows inline; content "px-1 py-1.5 transition-opacity duration-150" (no delay). Row button has no caret affordance. | Disclosure container tokens (160/120ms); row-tap OK but needs affordance decision per silent-grower rule. | 2 | small |
| 19 | `webapp/src/components/mobile/v2/movimientos/movimientos-transaction-row.tsx:66-75, 145-156` | movimientos-row-expand-instant | Row button aria-label "Ver acciones de X"/"Ocultar acciones de X" (copy compliant) but NO caret; expanded quick-actions mount INSTANTLY: {expanded && (<div className="mt-3"><TransactionQuickActions .../></div>)} — no animation at all. | Wrap in Disclosure container (grid 0fr->1fr @160ms + content @120ms); add caret affordance. | 2 | small |
| 20 | `webapp/src/components/mobile/v2/movimientos/movimientos-lectura.tsx:289-301` | movimientos-lectura-toggle-copy | Toggle text: {expanded ? "▴ Colapsar" : "▾ Ver flujo por día"} in "text-[10px] font-medium text-z-brass" — unicode arrows instead of caret icon, "Colapsar" instead of "Ocultar + qué", brass (banned); grid duration-200 + opacity 150 delay-75. | "Ver flujo por día"/"Ocultar flujo por día", 12px/600 sage-light, rotating ChevronDown 12px; 160/120ms. | 2 | trivial |
| 21 | `webapp/src/components/mobile/v2/movimientos/movimientos-herramientas.tsx:168-185` | movimientos-herramientas-zones | Zone stat-chips (aria-expanded on each chip, no caret) drive a shared panel: "grid transition-[grid-template-rows] duration-200 ease-out" + gridTemplateRows activeZone?1fr:0fr; content "mt-1.5 transition-opacity duration-150" delay-75. | Disclosure tokens; chip-as-trigger keeps aria but needs caret/affordance ruling. | 2 | small |
| 22 | `webapp/src/components/mobile/v2/plan/plan-budget-hero.tsx:105-126` | plan-budget-hero-expander | Footer has StateChip + ChevronDown "size-3.5 text-muted-foreground transition-transform" rotate-180 — caret yes, text label no; grid duration-200 + "mt-3 space-y-2 transition-opacity duration-150" delay-75. | Disclosure with label ("Ver categorías"/"Ocultar categorías"), tokens. | 2 | small |
| 23 | `webapp/src/components/mobile/v2/plan/plan-zone-chips.tsx:111-125` | plan-zone-chips-expander | "grid transition-[grid-template-rows] duration-200 ease-out" + gridTemplateRows hasActive?1fr:0fr; content "mt-1.5 transition-opacity duration-150" + delay-75; chip triggers with aria-expanded, no caret. | Disclosure tokens (160/120ms). | 2 | small |
| 24 | `webapp/src/components/mobile/v2/inicio/widget-grid.tsx:116-135` | widget-grid-expander | Widget chips expand a detail panel: "grid transition-[grid-template-rows] duration-200 ease-out" + gridTemplateRows active?1fr:0fr; content "transition-opacity duration-150" (pt-1 when active); sibling chips get dimmed opacity-40 (widget-chip.tsx:55). Comment line 58 documents the 200ms timing. | Disclosure tokens; chips keep active/dimmed states (not a page dim — in-flow opacity). | 2 | small |
| 25 | `webapp/src/components/mobile/v2/linked-metric-detail-panel.tsx:97-118` | linked-metric-detail-panel | "grid transition-[grid-template-rows] duration-200 ease-out" + gridTemplateRows activeChip?1fr:0fr; content "mt-2 transition-opacity duration-150" + "opacity-100 delay-75". | Disclosure tokens. | 2 | small |
| 26 | `webapp/src/components/mobile/cards/mobile-hero-card.tsx:273-285` | mobile-hero-card-collapse-panel | CollapsePanel: "grid transition-[grid-template-rows] duration-200 ease-out" + gridTemplateRows visible?1fr:0fr; content "mt-2 transition-opacity duration-150". Section chips at line ~260 use aria-expanded. | Disclosure tokens (legacy mobile/cards surface — audit if still mounted). | 2 | small |
| 27 | `webapp/src/components/mobile/cards/mobile-alert-card.tsx:99-115, 136` | mobile-alert-card-expander | Grid duration-200 + "rounded-xl border p-3 transition-opacity duration-150"; dismiss/collapse action labeled bare "Ocultar" (line 136). | Disclosure tokens; label "Ocultar + qué" (e.g. "Ocultar alerta"). | 2 | small |
| 28 | `webapp/src/components/mobile/cards/mobile-upcoming-payments.tsx:96-106` | mobile-upcoming-payments-expander | Per-item: "grid transition-[grid-template-rows] duration-200 ease-out" + gridTemplateRows isExpanded?1fr:0fr; content "mb-1 rounded-lg border border-white/8 bg-black/20 p-2.5 transition-opacity duration-150" + "opacity-100 delay-75". Row trigger has no caret. | Disclosure tokens + affordance. | 2 | small |
| 29 | `webapp/src/components/ui/collapsible.tsx:22-31` | radix-collapsible-no-motion | shadcn Collapsible wrapper: CollapsibleContent has NO animation classes — expand/collapse is instant everywhere it is used (5 call-sites). | Replace call-sites with <Disclosure /> (or give CollapsibleContent the 160ms grid-rows treatment) so all expanders share one idiom. | 2 | small |
| 30 | `webapp/src/components/transactions/transaction-form.tsx:331-354` | transaction-form-advanced-collapsible | Radix Collapsible "Opciones relacionadas" with ChevronDown className transition-transform + advancedOpen ? "rotate-180" (line 348); content instant (no animation). | Disclosure; label already descriptive. | 2 | trivial |
| 31 | `webapp/src/components/mobile/mobile-transaction-form.tsx:476-499` | mobile-transaction-form-advanced-collapsible | Radix Collapsible "Más opciones" + ChevronDown rotate-180 (line 493); instant content. | Disclosure. | 2 | trivial |
| 32 | `webapp/src/components/destinatarios/destinatario-suggestions-tab.tsx:177-197` | destinatario-suggestions-collapsible | Collapsible trigger "Ver transacciones" with STATIC ChevronRight h-3 w-3 (no rotation, wrong direction); no "Ocultar" state; instant content. | Disclosure: "Ver transacciones"/"Ocultar transacciones", rotating chevron-down. | 2 | trivial |
| 33 | `webapp/src/components/recurring/recurring-completed-section.tsx:32-43` | recurring-completed-collapsible | Collapsible trigger "N pagos completados este mes" + ChevronDown "transition-transform [[data-state=open]>&]:rotate-180"; instant content. Trigger styled z-income. | Disclosure ("Ver N pagados" pattern fits T6 group summary too). | 2 | trivial |
| 34 | `webapp/src/app/(dashboard)/import/page.tsx:128-132` | import-page-native-details | Native <details className="group ..."> + <summary> "Más sobre este flujo" with ChevronDown "transition-transform group-open:rotate-180" — instant expand. | Disclosure (or accept native details with 160ms treatment); copy OK-ish but not "Ver + qué". | 2 | trivial |
| 35 | `webapp/src/components/mobile/v2/plan/mobile-periodo-view.tsx:55-71` | periodo-view-instant-expand | "Ver flujo del mes" + ChevronDown rotate-180, but label NEVER flips to "Ocultar …" when open, and content mounts instantly via {chartOpen && ...}. | Disclosure: label toggles "Ver flujo del mes"/"Ocultar flujo del mes", 160ms container. | 2 | trivial |
| 36 | `webapp/src/components/mobile/v2/plan/mobile-recurrentes-templates-strip.tsx:44-69` | recurrentes-templates-bare-ver | Toggle label {expanded ? "Ocultar" : "Ver"} — BARE, in "text-[11px] font-medium text-z-brass" (banned brass), with ChevronUp/ChevronDown icon SWAP instead of rotation; content instant {expanded && ...}. | "Ver plantillas"/"Ocultar plantillas", sage-light, single rotating caret, Disclosure 160ms. | 2 | trivial |
| 37 | `webapp/src/components/mobile/v2/plan/mobile-recurrentes-view.tsx:527-535, 439` | recurrentes-completados-bare-ver | Completados toggle: <span>{show ? "Ocultar ↑" : "Ver ↓"}</span> — bare Ver with unicode arrows, instant {show && ...}. Also line 439: showAllLabel="Mostrar todas las transacciones →" (banned "Mostrar"). | "Ver N completados"/"Ocultar completados" + rotating caret + Disclosure; rename showAll label to "Ver todas las transacciones". | 2 | trivial |
| 38 | `webapp/src/components/mobile/v2/plan/plan-mobile-accounts-card.tsx:29-62` | plan-accounts-card-silent-grower | Whole card <button> aria-label "Mostrar saldos de cuentas principales" (banned "Mostrar") / "Ocultar saldos …"; NO caret or visible label; content instant {expanded && ...}; card uses "transition-all" + brass border state. | Disclosure "Ver cuentas"/"Ocultar cuentas" with caret, 160ms. | 2 | small |
| 39 | `webapp/src/components/personas/persona-card.tsx:105-115` | persona-card-instant | ChevronDown "size-4 shrink-0 text-muted-foreground transition-transform" rotate-180; detail instant {open && ...}. | Disclosure 160ms container. | 2 | trivial |
| 40 | `webapp/src/components/reminders/pendientes-widget.tsx:45-57` | pendientes-completados-instant | "Completados (N)" + ChevronDown h-3 w-3 rotate-180; instant {showCompleted && ...}. | Disclosure ("Ver N completados"). | 2 | trivial |
| 41 | `webapp/src/components/cashflow-planner/expense-entry-row.tsx:192-200` | expense-entry-row-instant | ChevronDown rotate-180 caret; detail instant {expanded && (...)}. | Disclosure 160ms container. | 2 | trivial |
| 42 | `webapp/src/components/cashflow-planner/income-envelope-card.tsx:229-237` | income-envelope-toggle | Toggle text {expanded ? "Ocultar asignaciones" : `Ver ${n} asignación(es)`} — copy compliant but NO caret, "text-[10px] text-muted-foreground", instant swap (no animated container in file). | Disclosure: keep copy, add 12px caret + 160ms container, 12px/600 sage-light. | 2 | trivial |
| 43 | `webapp/src/components/cashflow-planner/envelope-board.tsx:120-175` | envelope-board-settled-instant | "N confirmados"/"N pagados" settled toggles with ChevronDown rotate-180 (lines 131-136, 166-171); no grid-rows container in file — lists swap instantly. | Disclosure containers; labels already count-based (aligns with T6 "N de M pagados"). | 2 | trivial |
| 44 | `webapp/src/components/import/step-review.tsx:640-660, 770-790` | import-step-review-instant | Two expanders: label {expanded ? "Ocultar detalle" : "Ver y ajustar selección"} + ChevronDown rotate-180 (646-654); currency-group header + ChevronDown rotate-180 (777-782); both instant {expanded && ...}. | Disclosure 160ms; copy already compliant. | 2 | trivial |
| 45 | `webapp/src/components/import/credit-card-stack-card.tsx:137-150` | import-misc-expanders-instant | "Ver detalles"/"Ocultar detalles" + ChevronDown h-3 w-3 rotate-180, instant {expanded && <dl ...>}. Same instant+caret pattern in import/suggested-destinatarios-panel.tsx:141-150, import/pending-email-statements.tsx:231-240 ({open && <CardContent ...>}), import/reconcile-chip.tsx:85-92 (caret bottom-right of stat chip). | All four -> Disclosure 160ms containers with labels. | 2 | small |
| 46 | `webapp/src/components/destinatarios/destinatario-list.tsx:504-513` | destinatario-list-row-expand | Row-name ChevronDown "size-4 shrink-0 text-muted-foreground transition-transform" rotate-180; expansion instant (no grid-rows container in file). | Disclosure 160ms container. | 2 | trivial |
| 47 | `webapp/src/lib/constants/styles.ts:158-163` | ver-todas-brass-constant | INLINE_EXPAND_TOGGLE_CLASS = "flex w-full items-center justify-center gap-1 border-t border-white/6 py-2 text-[11px] font-semibold text-z-brass transition-colors hover:bg-white/[0.02]" — brass disclosure label (banned). Consumers: tendencias/category-trend-list.tsx:301 ({showAll ? "Ver menos" : `Ver todas (${n})`}) and tendencias/top-recipients-card.tsx:173 (same copy), both with ChevronDown size-3 rotate-180. ROW_EXPAND_TRIGGER_CLASS (162-163) is the compliant row-trigger recipe. | Rebind to 12px/600 sage-light hover:white; copy -> "Ver N categorías"/"Ver N destinatarios" and "Ocultar …" instead of "Ver menos". | 2 | trivial |
| 48 | `webapp/src/components/plan/plan-flow-timeline.tsx:118-131` | plan-flow-timeline-ver-mas | Toggle {expanded ? "Ver menos" : `Ver ${n} más`} in "text-[11px] font-semibold text-z-brass" + ChevronDown rotate-180; list slice swaps instantly. | "Ver N más"/"Ocultar N" (Ocultar + qué), sage-light not brass, Disclosure reveal. | 2 | trivial |
| 49 | `webapp/src/components/gestionar/attention-hub.tsx:71` | bare-ver-links | <span className="text-sm font-medium text-muted-foreground">Ver →</span> — bare "Ver" on suggestion cards. Related: mobile/v2/inicio/inicio-attention-timeline.tsx:109 "Ver todo" (10px, aria says "Ver todos los pendientes en la bandeja"). | Copy rule 2: "Ver + qué" (e.g. "Ver sugerencias", "Ver pendientes"). Never bare "Ver". | 2 | trivial |
| 50 | `webapp/src/app/(dashboard)/transactions/[id]/transaction-detail-client.tsx:951-959` | ver-mapa-brass | {mapOpen ? "Ocultar mapa" : "Ver mapa"} — copy compliant but styled "text-[11px] font-semibold text-z-brass hover:underline" (brass banned on disclosure labels); map mounts instantly {mapOpen && ...}. | Sage-light label + caret + Disclosure container. | 2 | trivial |
| 51 | `webapp/src/components/ui/sheet.tsx:31-45, 60-73` | sheet-timing-backdrop | SheetOverlay: "data-[state=open]:animate-in … fade-in-0 fixed inset-0 z-[var(--z-layer-modal)] bg-black/50" (tw-animate default ~150ms, no blur). SheetContent: "transition ease-in-out data-[state=closed]:duration-300 data-[state=open]:duration-500" + slide-in-from-right/left/top/bottom. | Spec: sheet 200ms (--motion-3), backdrop rgba(0,0,0,0.6) + blur(4px) fading @200ms. | 2 | small |
| 52 | `webapp/src/components/ui/drawer.tsx:24-48` | drawer-vaul-timing-backdrop | DrawerOverlay: "fixed inset-0 z-[var(--z-layer-modal)] bg-black/50" (no blur, vaul default 500ms cubic-bezier(0.32,0.72,0,1) transition). Content: vaul default slide (500ms). Handle (line 46): "mx-auto mt-3 mb-2 h-1 w-10 shrink-0 rounded-full bg-muted-foreground/30" (40x4 but muted-foreground/30, not white/12). | translateY(105%)->0 @200ms --motion-ease; backdrop rgba(0,0,0,0.6)+blur(4px) @200ms; handle 40x4 radius 2 white/12. | 2 | small |
| 53 | `webapp/src/components/mobile/fab-menu.tsx:93-106` | fab-menu-sheet | Movimientos FAB sheet (raw vaul): Overlay "fixed inset-0 bg-black/50" + FAB_MENU_Z; content vaul default 500ms; handle "mx-auto mt-3 mb-2 h-1 w-10 shrink-0 rounded-full bg-muted-foreground/30". T3 explicitly names this surface. | 200ms sheet + 0.6/blur(4px) backdrop + white/12 handle (same as drawer.tsx). | 2 | small |
| 54 | `webapp/src/components/ui/dialog.tsx:39-48, 64-70` | dialog-backdrops | DialogOverlay "bg-black/50" + fade animate-in/out (no blur); DialogContent "data-[state=closed]:zoom-out-95 data-[state=open]:zoom-in-95 … duration-200" (scale entrance). alert-dialog.tsx:39 identical bg-black/50 overlay. | Backdrop rgba(0,0,0,0.6)+blur(4px) @200ms (dim=modal is correct here); consider replacing zoom-95 scale with opacity/translate to honor "nothing springs/scales". | 2 | small |
| 55 | `webapp/src/components/mobile/v2/mobile-tab-bar.tsx:80` | press-scale-purge | FAB: "transition-transform active:scale-95". Other press/hover scales: components/categories/zone-tile.tsx:36 "hover:scale-[1.02] active:scale-[0.98]"; components/categories/color-picker.tsx:27 "transition-transform hover:scale-110"; components/debt/salary-bar.tsx:70 "hover:brightness-110 hover:scale-y-105"; components/dashboard/hybrid-hero.tsx:531 "active:scale-95". | Never press-scale — hover-tint only (T3 dim rule bans press scale explicitly). | 2 | small |
| 56 | `webapp/src/components/mobile/v2/mobile-tab-bar.tsx:83` | fab-plus-rotation | Plus icon: "size-5 stroke-[2.5] transition-transform duration-200" + rotate-45 when FAB menu open — a non-caret rotating element (spec: the disclosure caret is "the ONLY rotating element in the app"). | Decision: exempt (overlay affordance) or replace with icon swap/opacity. | 2 | trivial |
| 57 | `webapp/src/components/accounts/flip-zone.tsx:30, 80` | off-token-durations | "relative w-full transition-transform duration-400" (card flip) + "transition-all duration-300". Other 300ms sites: app/onboarding/page.tsx:238,247 ("transition-all duration-300" progress bars); components/dashboard/primeros-pasos-card.tsx:85 ("transition-[width] duration-300"); components/dashboard/interactive-metric-card.tsx:113 ("transition-all duration-300"); components/budget/dashboard-budget-bar.tsx:30 ("transition-all duration-300"). Census: 30x duration-200, 20x duration-150, 7x duration-300, 1x duration-400, 1x duration-500 (sheet), 1x duration-75. | Clamp everything to 120/160/200 (--motion-1/2/3), one curve; nothing longer. | 2 | small |
| 58 | `webapp/src/components/mobile/v2/expand.tsx:19` | content-enter-150ms-pattern | App-wide content-enter idiom is "transition-opacity duration-150" + "opacity-100 delay-75" (~20 call-sites: pulse-widget:225, burndown-expandable:94, mobile-hero-card:281, mobile-upcoming-payments:103, expandable-card:46, deudas-hero:98, linked-metric-detail-panel:105, mobile-alert-card:107, plan-zone-chips:118, inicio-activity:236, movimientos-herramientas:175, deudas-salary-bar:63, inicio-burndown:275, plan-budget-hero:124, widget-grid:125, movimientos-lectura:300). Also "animate-in fade-in duration-150" at categorize/inbox-transaction-row.tsx:113 and dashboard/debt-free-banner.tsx:22. | Content enters @120ms (--motion-1) opacity + translateY(-3px)->0 inside <Disclosure /> — replaces the 150ms+delay-75 choreography everywhere. | 2 | medium |
| 59 | `webapp/src/components/dashboard/speedometer-gauge.tsx:95` | chart-motion-violation | Gauge needle dot: transition: "cx 0.6s ease, cy 0.6s ease, fill 0.6s ease" — 600ms animation of non-transform SVG attributes on a chart (charts must stay at zero motion). | Remove transition (charts stay at zero; the only 600ms allowed is the T4 celebration ring sweep). | 3 | trivial |
| 60 | `webapp/src/components/ui/skeleton.tsx:4-12` | skeleton-shimmer-check | Skeleton = "bg-accent animate-pulse rounded-md" — uniform Tailwind pulse, no shimmer/stagger choreography found anywhere (grep shows no custom shimmer keyframes). Loading spinners use animate-spin (Loader2) across ~40 files. | Already compliant with "no skeleton shimmer choreography"; keep animate-pulse; document as the sanctioned loading state. | 2 | trivial |
| 61 | `webapp/package.json:1` | no-framer-motion | Zero framer-motion/motion imports in webapp/src (grep clean) and framer-motion is not a webapp dependency — all motion is CSS transitions + tw-animate-css utilities. | Nothing to remove — satisfies "cero librerías nuevas"; Disclosure ships as pure CSS. | 2 | trivial |

### charts — 35 findings

| # | File | Topic | Current | Target | Phase | Effort |
|---|---|---|---|---|---|---|
| 1 | `webapp/src/app/globals.css:183-187, 220-224` | chart-vars-rebind | --chart-1: var(--z-income); --chart-2: var(--z-expense); --chart-3: var(--z-alert); --chart-4: var(--z-debt); --chart-5: var(--z-brass); (duplicated in :root and .dark) | Spec mapping: --chart-1 → --z-brass (actual/tú), --chart-2 → --z-sage-dark (ideal/promedio), --chart-3 → --z-income, --chart-4 → --z-expense, --chart-5 → --z-debt. Rebinding silently recolors every --chart-* consumer (daily-spending uses chart-5=brass, burn-rate uses chart-1=income, debt-simulator uses chart-1/2/3, planner PLAN_COLORS uses chart-1/2/3, detail-step cycles chart-1..5) — each consumer must be re-audited at rebind time. | 3 | medium |
| 2 | `webapp/src/components/ui/chart.tsx:1-366` | charttheme-home | shadcn chart wrapper: ChartContainer (injects per-chart --color-{key} vars via ChartStyle), ChartTooltip/ChartTooltipContent (border-border/50 bg-background), ChartLegend/ChartLegendContent (legend boxes with h-2 w-2 rounded-[2px] swatches). No shared grid/axis/series defaults exist anywhere — every chart re-declares them. | Single chartTheme object (e.g. new webapp/src/lib/constants/chart-theme.ts or exported from chart.tsx, which every recharts file already imports): grid={vertical:false, stroke:'rgba(255,255,255,0.06)'}, axis={axisLine:false, tickLine:false, tick:{fontSize:10, fill:'var(--z-sage-dark)'}}, series tokens chart-1..5 per spec, forecast dash '4 3' @50%, tabular compact COP tickFormatter ($3,76M/$178K), isAnimationActive:false, compact tooltip (#111 ≈ bg-z-surface-2, border white/6). Each of the ~16 live recharts files consumes it instead of local constants. | 3 | medium |
| 3 | `webapp/src/components/tendencias/forecast-card.tsx:10, 28-38` | tendencias-forecast-card | LineChart; config color 'var(--z-brass-hot)'; <Line stroke='var(--color-balance)' strokeWidth={2.2} strokeDasharray='4 4' dot={{ r: 2.5 }} isAnimationActive={false} />; XAxis tickLine/axisLine false fontSize 10; no grid, no YAxis; months via monthShort() (compliant 'abr' style); first point labeled 'hoy'. | chartTheme forecast recipe: dashed '4 3' at 50% opacity, hue --z-brass (not brass-hot), dot={false} everywhere except a single 3px 'hoy' marker; horizontal hairlines white/6 if any. | 3 | small |
| 4 | `webapp/src/components/tendencias/income-expense-card.tsx:10-13, 30-48` | tendencias-income-expense-card | BarChart; income 'var(--z-income)', expense 'var(--z-expense)', radius [3,3,0,0], isAnimationActive false; no grid; custom tooltip 'rounded-lg border bg-background p-2 text-xs shadow-sm'; no legend. | Nearly compliant — consume chartTheme: paired bars 12px gap 3, history 0.8 opacity / current full, tooltip from theme (#111 border white/6), horizontal hairlines max 3. | 3 | trivial |
| 5 | `webapp/src/components/tendencias/savings-rate-card.tsx:2, 8-11, 29-41` | tendencias-savings-rate-area | BANNED AreaChart: <Area stroke='var(--color-rate)' fill='url(#srGrad)'> with linearGradient stopOpacity 0.4→0.03 on 'var(--z-brass-hot)'; ReferenceLine y={20} stroke='var(--color-target)' (--z-income) strokeDasharray='3 3'; header '{current}%' in text-z-brass. | Area → Line (brass); reference line = sage-dark dashed per theme (reference/promedio hue is sage-dark, income green reserved for income-vs-expense frames); remove gradient defs. | 3 | small |
| 6 | `webapp/src/components/dashboard/burn-rate-card.tsx:27-29, 36-46, 156-216` | burn-rate-forecast-card | BANNED AreaChart (dashboard 'Ritmo de gasto' burndown): balance Area fill url(#gradientId) stops 'var(--chart-1)' 0.4→0.05; projected Area stroke 'var(--chart-1)' strokeDasharray='4 3' fill='none'; ReferenceLine y=0 stroke='hsl(var(--destructive))' (legacy hsl() wrap — invalid color under Tailwind v4 tokens); obligation ReferenceLines stroke='var(--z-expense)' dasharray '2 2' with fontSize 9 labels fill 'var(--z-sage-dark)'; YAxis tickFormatter={(v) => formatCurrency(v, data.currency)} width 60 (full COP, not compact); trendColor = {accelerating:'text-red-400', stable:'text-zinc-400', decelerating:'text-green-400'} raw Tailwind palette verdict on chart card. | Area→Line burndown per spec (actual brass 1.75, projection dashed '4 3' 0.5 same hue, hoy dot r=3); chart-1 rebinds to brass; y=0 baseline solid white/12; compact tabular COP ticks; trend verdict moves to T1 Verdict slot, not colored chart text. | 3 | medium |
| 7 | `webapp/src/components/charts/budget-pace-chart.tsx:98-127, 133-142` | budget-pace-verdict-on-chart | LineChart (dashboard Presupuesto section): ideal line stroke '#a1a1aa' HARDCODED HEX strokeDasharray '4 4'; actual line stroke={overBudgetPace ? 'var(--z-debt)' : 'var(--z-income)'} (verdict-colored series on Tier-2 chart); ReferenceDot r=4 stroke='white' hardcoded; badge below: className={overBudgetPace ? 'text-z-debt' : 'text-z-income'} '{spent} de {budget} — {progress}%'. | Ideal = sage-dark dashed '2 3' 0.8; actual = brass 1.75 always (never verdict-colored); hoy dot 3px brass; judgment moves to hero <Verdict />. Deletes #a1a1aa and stroke='white'. | 3 | small |
| 8 | `webapp/src/components/charts/category-donut.tsx:3, 17-29, 56-72` | category-donut-pie-banned | BANNED PieChart/Pie/Cell donut (dashboard presupuesto-section.tsx:85): innerRadius 65% outerRadius 95% paddingAngle 2; COLORS = ['var(--z-expense)','var(--z-alert)','var(--z-income)','var(--z-sage-dark)','var(--z-sage-dark)','var(--z-sage)'], OTROS_COLOR 'var(--z-sage-muted)'; legend dots h-2 w-2 rounded-full. | Pie → horizontal bars (Tendencias bar recipe: 26px radius 3 3 0 0 or split-bar rows); keys as inline 8×8 radius-2 swatch chips; drop multi-hue category palette for theme hues. | 3 | medium |
| 9 | `webapp/src/components/charts/cash-flow-view-toggle.tsx:105-157, 161-194, 196-278` | cash-flow-view-toggle | Dashboard Flujo card (mounted via flujo-charts.tsx). Line view: income solid 'var(--z-income)' w2, expenses dashed '6 3' 'var(--z-expense)' w2, ReferenceDots r=4 stroke='white'; YAxis hide. Bar view: CartesianGrid vertical={false} (default #ccc stroke restyled by chart.tsx to stroke-border/50), ChartLegend legend box, radius [4,4,0,0], YAxis '1.2M'/'350k' ticks. Below chart: 'Ahorro: {rate}%' colored text-z-income/text-z-debt + ↑/↓ arrows (verdict on chart). | Consume chartTheme: hairlines white/6, expenses dash aligned to spec dashes, no legend box (inline swatch chips), tabular compact COP ($3,76M), no stroke='white' dots; Ahorro verdict moves to hero Verdict slot. | 3 | small |
| 10 | `webapp/src/components/charts/balance-history-chart.tsx:1-205` | orphan-chart-components | ZERO importers for 5 chart files: balance-history-chart.tsx, monthly-cashflow-chart.tsx, daily-spending-chart.tsx, enhanced-cashflow-chart.tsx, income-vs-expenses-chart.tsx (verified by import grep — nothing outside components/charts/ references them). They contain the worst offenders: 'hsl(var(--primary))' configs (balance-history:33, net-worth pattern), hardcoded '#3b82f6'/'#8b5cf6' (enhanced-cashflow:44-45), expenses colored var(--z-debt) (enhanced-cashflow:42), dashed CartesianGrid '3 3', es-CO axis labels 'jun. 25'. | Delete the 5 orphans instead of migrating them — removes ~1000 lines of off-spec chart code from the bundle surface. | 3 | trivial |
| 11 | `webapp/src/components/charts/net-worth-history-chart.tsx:24-29, 54-100` | net-worth-history-area | BANNED AreaChart (mounted by dashboard patrimonio-section.tsx:53): chartConfig color 'hsl(var(--primary))' — legacy shadcn hsl-wrap; --primary is var(--z-brass) (already a full color), so hsl(var(--primary)) parses as an INVALID color; gradient fillNetWorth 0.3→0; CartesianGrid vertical={false} strokeDasharray='3 3'; YAxis '1.2M/350k' ticks. | Area → Line stroke var(--chart-1) (brass post-rebind); fix broken hsl() wrap; solid white/6 hairlines (no dashes); tabular compact COP ticks. | 3 | small |
| 12 | `webapp/src/components/charts/waterfall-chart.tsx:69-95` | waterfall-chart-div-bars | Hand-rolled div-bar waterfall (dashboard flujo-waterfall.tsx:35): bars bg-z-income (Ingresos), bg-z-expense (categories), 'border border-z-income bg-z-income/10' (Neto); 10px bold value labels above every bar. | Compliant with bars-only rule; align via theme: label only decision numbers (peak + current) instead of all bars, track/rest hues from theme. | 3 | trivial |
| 13 | `webapp/src/components/charts/spending-heatmap.tsx:10-16` | spending-heatmap-colors | levelColors = ['var(--z-surface-3)', 'color-mix(in srgb, var(--z-expense) 25%, var(--z-surface-3))', …45%, …65%, 'var(--z-expense)'] — expense-orange intensity ramp (dashboard ActividadHeatmap). | Token-based ramp is on-brand; keep but source ramp from chartTheme so heatmap and future heatmaps share one scale. | 3 | trivial |
| 14 | `webapp/src/components/debt/debt-simulator.tsx:172-181, 283-345, 362-366, 542-627` | debt-simulator-areas | Two BANNED AreaCharts on /deudas simulator: (1) single-account 'Proyección de saldo' — withoutExtra 'var(--chart-1)' / withExtra 'var(--chart-2)' with fillWithout/fillWith gradients 0.3→0.05; (2) strategies chart — strategyChartConfig {baseline:'var(--chart-3)', snowball:'var(--chart-1)', avalanche:'var(--chart-2)'}, baseline dashed '4 4' w1.5, gradients; both use CartesianGrid vertical={false} (default), ChartLegend boxes, XAxis label 'Meses', YAxis '1.2M/350k'. | Area → Line; max 2 series + 1 reference per chart (3-series strategy chart needs baseline as reference); colors from rebound chart vars (brass=chosen plan, sage-dark=comparison); inline swatch chips, no legend boxes. | 3 | medium |
| 15 | `webapp/src/components/debt/planner/compare-step.tsx:326-422` | debt-planner-compare-areas | BANNED overlay AreaChart (planner Comparar step): baseline Area stroke 'var(--muted-foreground)' strokeDasharray '5 5' fill url(#fillBaseline) 0.1→0.02; up to 3 plan Areas stroke PLAN_COLORS[i] fill gradients 0.3→0.05; ChartLegend box; tick fontSize 11. PLAN_COLORS = ['var(--chart-1)','var(--chart-2)','var(--chart-3)'] (planner/utils.ts:3-7). | Area → Line overlay; baseline = sage-dark dashed reference; plans capped at theme series hues (brass + sage-dark + one semantic); kill gradients + legend box. | 3 | small |
| 16 | `webapp/src/components/debt/planner/detail-step.tsx:168-177, 519-632` | debt-planner-detail-stacked-area | BANNED stacked AreaChart per account: cfg color `var(--chart-${(i % 5) + 1})` cycling all 5 chart vars (per-card palette), gradient defs per account 0.3→0.05, stackId='accounts', hand-rolled legend with rounded-full dots also `var(--chart-${(i % 5) + 1})` (lines 590, 612, 627). | Stacked area → grouped/stacked bars or line per spec (lines+bars only); max 2 series + 1 reference — per-account trajectories need a different encoding (e.g. T2 split-bar rows per debt); palette cycling deleted. | 3 | large |
| 17 | `webapp/src/components/debt/salary-timeline-chart.tsx:34-49, 197-278, 284-303, 326-339` | salary-timeline-foreign-hues | Stacked BarChart (planner detail): fills from getDebtColor() — DEBT_PALETTE ['#dc2626','#ea580c','#d97706','#7c3aed','#6366f1','#0891b2'] + LIBRE_COLOR '#22c55e' hardcoded hex in packages/shared/src/utils/salary-breakdown.ts:32-41 (purple/indigo/cyan = foreign hues); axis months SPANISH_MONTHS ['Ene','Feb',…] capitalized (violates 'abr' lowercase ladder); cascade segments fillOpacity 0.4; summary banner 'rounded-lg bg-green-50/60 border-green-200/50 text-green-800' (light-mode greens on dark app); hand-rolled legend dots. | Palette from chartTheme tokens (debt hues limited to --z-debt ramp/opacity steps, libre = --z-income); lowercase 3-letter axis months; banner → surface-income recipe; shared-package palette change coordinated with mobile consumers. | 3 | medium |
| 18 | `webapp/src/components/accounts/balance-graph-hero.tsx:34, 45-56, 64-104` | accounts-area-heroes | BANNED AreaChart on account detail hero: strokeColor = isDebt ? 'var(--z-brass)' : 'var(--z-sage-light)'; gradient 0.3→0; tick {fontSize:9, fill:'var(--z-sage-dark)'}; Tooltip cursor stroke 'rgba(255,255,255,0.1)'; trend pill 'bg-emerald-500/15 text-z-income' / 'bg-red-500/15 text-red-400' with ▲/▼ (verdict pill on chart, raw emerald/red palette). Same pattern in graph-face.tsx:63-103 (fill graphFaceFill brass 0.3→0, same trend pill at 47-58) and spending-pulse-hero.tsx:56-75 (spendingPulseFill brass 0.2→0). | Area → Line brass (actual); trend verdict → T1 tokens (income/debt surface recipe) or removed from chart frame; axis fills/ticks from chartTheme. | 3 | small |
| 19 | `webapp/src/components/accounts/chart-utils.tsx:17-26` | accounts-axis-format | formatAxisDate: toLocaleDateString('es-CO', { day:'numeric', month:'short' }) → '3 jun.' (ICU adds trailing period, violates 'abr' no-period rule); formatAxisAmount: '1.2M'/'350k' (no $ prefix, lowercase k, not the $3,76M/$178K ladder); ChartTooltip: bg-z-surface-2 border-white/6 (closest to spec tooltip already). | Axis date via date-fns es 3-letter lowercase no period; amounts as tabular compact COP $3,76M/$178K from chartTheme formatter; promote this tooltip style into the shared theme. | 3 | trivial |
| 20 | `webapp/src/components/charts/sparkline.tsx:6-11, 28-38` | sparkline-component | Recharts AreaChart sparkline (dashboard accounts-overview rows): colorMap positive/warning/danger = z-income/z-expense/z-debt strokes with color-mix 12% fills, neutral = '#a1a1aa' + '#a1a1aa20' HARDCODED; verdict-colored per account (accounts-overview.tsx:213-215 picks color by utilization/change). | Area → bare Line sparkline, no fill; row sparklines are evidence not verdicts — single quiet hue (sage-dark or white/25) per T6; delete #a1a1aa. | 3 | small |
| 21 | `webapp/src/components/dashboard/speedometer-gauge.tsx:36-46, 56-123` | speedometer-gauge-tier1 | Hand-rolled 180° semicircle gauge (health score, mounted by health-score-section.tsx): viewBox '0 0 200 120', R=80, STROKE_W=12, four colored zone arcs ZONES = z-debt/z-alert/z-income/z-excellent at 0.85 opacity, indicator circle r=6 with CSS transition 'cx 0.6s ease, cy 0.6s ease, fill 0.6s ease' (animated — violates zero chart motion), center <text> 28px/700 fill currentColor + 11px label fill var(--color-muted-foreground). | Tier-1 270° ring: viewBox 0 0 128 128, path 'M 27.23 100.77 A 52 52 0 1 1 100.77 100.77', pathLength=100, dasharray '{pct} 100', 8-9px stroke round caps, track white/6, fill brass (→ --z-debt only ≥100), sage-dark 'hoy' tick, center 26px/800 hero number; no transition (except the 600ms celebration sweep in Fase 4). | 3 | medium |
| 22 | `webapp/src/components/mobile/v2/progress-ring.tsx:26-45` | progress-ring-mobile-v2 | 44px full-circle ring (deudas lenses tiles): viewBox '0 0 40 40' -rotate-90, r=16 strokeWidth 3, track className 'stroke-white/6', tones TONE_STROKE brass/income/debt, strokeDasharray=circumference + dashoffset, round caps. Used by deudas-plan-lens.tsx and deudas-cuentas-lens.tsx. | Candidate to become THE shared Tier-1 ring component (geometry → 270° pathLength=100 spec at 96/128px sizes); max one gauge per screen — audit deudas lenses for multiples. | 3 | small |
| 23 | `webapp/src/components/mobile/v2/inicio/widgets/ritmo-widget.tsx:20-53` | ritmo-widget-ring | ArcRing: 60px full-circle, r=24 strokeWidth 5, track 'var(--color-z-surface-3)', fill 'var(--color-z-income)' (income green as gauge fill — spec says gauge fill = brass), rotate(-90), center 13px bold '{percentage}%'. RITMO is explicitly named a Tier-1 ring surface in the spec. | Replace with shared Tier-1 270° ring: track white/6, fill brass, sage-dark hoy tick, center hero number + micro-label. | 3 | small |
| 24 | `webapp/src/components/debt/debt-quick-stats.tsx:18-52` | debt-utilization-ring | UtilizationRing (deudas quick stats): 48px full-circle r=20 strokeWidth 4, track stroke 'hsl(var(--muted))' (legacy hsl-wrap — invalid color), fill stepped: ≤30 var(--z-income), ≤70 var(--z-alert), else var(--z-debt); center % text colored same. | Shared Tier-1 ring geometry; fill brass → debt at te-pasaste threshold only (color steps replaced by T1 verdict logic pct≥100 te-pasaste / ≥87 cerca); fix hsl() wrap. | 3 | small |
| 25 | `webapp/src/components/mobile/cards/mobile-budget-ring.tsx:131-157` | mobile-budget-ring | ProgressRing 38px full-circle strokeWidth 3, track 'rgba(255,255,255,0.06)', stroke = isOver ? 'var(--z-debt)' : isHigh ? 'var(--z-brass)' : 'var(--z-sage-light)'; tile also has stateCopy 'Sobre el límite'/'Último tramo'/'Con margen' colored text (verdict vocabulary outside the closed T1 set). | Shared ring (fill brass, debt ≥100 only — the 80-99% brass tier collapses); stateCopy converges to T1 vocabulary (Vas bien/Cerca del límite/Te pasaste). | 3 | small |
| 26 | `webapp/src/components/dashboard/runway-mini-chart.tsx:151-152, 164-183, 222-257` | runway-mini-chart-burndown | Hand-rolled SVG burndown (dashboard burndown-expandable + ritmo-widget detail): ideal line stroke 'var(--z-olive-deep)' strokeDasharray '4,3'; actual path brass 1.5; projected slope stroke 'var(--z-debt)' dasharray '3,2' (projection in red, different hue than series); midline 'var(--z-olive-deep)' dasharray '2,3' 0.4; custom SVG tooltip rect fill 'rgba(0,0,0,0.8)' stroke brass; 7-8px system-ui text labels. | Spec burndown: ideal dashed '2 3' sage-dark 0.8; actual brass 1.75; projection dashed '4 3' 0.5 SAME hue as series (brass, not debt); hoy dot r=3; olive-deep → sage-dark. | 3 | small |
| 27 | `webapp/src/components/mobile/cards/mobile-spending-pace.tsx:175-193` | mobile-spending-pace-burndown | RunwayChart SVG: gridlines stroke 'var(--z-surface-3)' 0.5; ideal 'var(--z-olive-deep)' dasharray '4,3' 1.5; actual brass 2; projected 'var(--z-debt)' dasharray '3,2'; dot brass r=3.5 + halo 5.5. | Same burndown recipe as runway-mini-chart (ideal sage-dark '2 3' 0.8, projection brass '4 3' 0.5, hairlines white/6) — one shared burndown, two mounts. | 3 | small |
| 28 | `webapp/src/components/mobile/v2/inicio/inicio-burndown.tsx:96-135, 159-183` | inicio-burndown-inverted-hues | BurndownChart SVG: axis lines stroke '#2a2d28' HARDCODED (spec: no axis lines); expected/ideal line stroke 'var(--color-z-brass)' dashed '6,4' 0.6 and ACTUAL line stroke 'var(--color-z-income)' — hue roles INVERTED vs spec (brass must be actual, sage/sage-dark comparison); on-chart deviation badge rect+text fill isUnder ? var(--color-z-income) : var(--color-z-debt) reading '{amount} bajo/sobre' — verdict chip on Tier-2 chart (banned). | actual → brass 1.75, ideal → sage-dark dashed '2 3' 0.8; delete axis lines and #2a2d28; deviation verdict moves to the card's T1 Verdict slot. | 3 | small |
| 29 | `webapp/src/components/mobile/v2/plan/plan-flow-chart.tsx:204-250, 296-334, 337-362, 422-430` | plan-flow-chart | Plan 'FLUJO DEL MES' SVG: grid 'rgba(255,255,255,0.03)'; zero line brass 0.4 + '$0' brass label (spec: zero baseline = solid white/12, brass reserved for series); expense bars fill 'var(--z-debt)' (debt red for ordinary expenses — spec: expense orange, debt red only for debt); dangerGrad stopColor '#ef4444' HARDCODED; danger chip 'border-red-500/15 bg-red-500/5 text-red-400' raw palette; cumulative balance polylines brass solid past / brass dashed '4,3' 0.5 future (compliant). | Grid → white/6 hairlines; zero line white/12; expense bars → --z-expense; #ef4444/red-500 → --z-debt tokens via surface recipe; danger verdict text moves toward T1 detail line. | 3 | small |
| 30 | `webapp/src/components/mobile/v2/movimientos/movimientos-lectura.tsx:141-199, 210-218` | movimientos-lectura-brass-expense | FlowChart SVG (Movimientos lectura mode): income polyline 'var(--color-z-income)' solid 2.5; expense polyline 'var(--color-z-brass)' dashed '6 4' — brass used as EXPENSE hue (spec: brass = what you did/actions, expense = --z-expense); 'Hoy' axis label 'font-semibold text-z-brass'; 7-8px axis text fill-muted-foreground. | Expense line → --z-expense; income/expense pairing per spec (only frame where both greens+orange meet); Hoy label sage-light per axis ladder; dash from theme. | 3 | small |
| 31 | `webapp/src/components/dashboard/hybrid-hero.tsx:51-55, 436-490` | hero-sparkline-verdict-tone | HeroSparkline polyline stroke = TONE_STROKE[tone] where TONE_STROKE = {income:'var(--color-z-sage)', alert:'var(--color-z-alert)', debt:'var(--color-z-debt)'} — sparkline recolors by verdict state (comment: 'Te pasaste hero shows a red rhythm'). Spec: charts explain, never verdict-bearing; verdict lives only in the T1 chip. | Sparkline fixed quiet hue (sage-dark); verdict expressed solely by the adjacent <Verdict /> chip in the hero slot. | 3 | trivial |
| 32 | `webapp/src/components/tendencias/category-trend-list.tsx:30-42, 74, 85-87` | category-trend-sparkline-palette | Row Sparkline (Tendencias gastos lens, desktop-only) polyline stroke={color} = per-category color (multi-hue per-row palette on charts); row color dot 'size-2.5 rounded' style background: color. | Row sparklines are V3 evidence — single hue from theme (sage-dark or white/25); category identity stays in the dot, not the chart stroke. | 3 | trivial |
| 33 | `webapp/src/components/mobile/v2/inicio/pulse-widget.tsx:73-135, 170-171` | pulse-widget-sparkline-area | Hand-rolled sparkline with BANNED area fill: gradient stopOpacity 0.25→0 + polyline; sparkColor = status.tone === 'brass' ? 'var(--color-z-brass)' : 'var(--color-z-sage)' (verdict-toned); status label 'En ritmo'/'Arriba del ritmo' colored dot+text (non-canonical verdict vocabulary). | Drop area fill (line only), fixed sage hue; status converges to T1 Verdict (Vas bien / Cerca del límite). | 3 | trivial |
| 34 | `webapp/src/actions/charts.ts:211, 264, 398, 676` | axis-month-format-ladder | Month labels feeding chart axes: formatDate(new Date(m + '-15'), 'MMM yyyy') → 'jun 2026' (month+year on axis; spec axis = 'abr' 3-letter only) and formatDate(dateStr, 'dd MMM') → '03 jun'. Tendencias format.ts monthShort() is the only compliant implementation ('2026-06' → 'jun'). | One axis formatter in chartTheme: 3-letter lowercase month, no period, no year (current month styled sage-light/600); promote monthShort into the theme. | 3 | small |
| 35 | `webapp/src/components/ui/chart.stories.tsx:1` | chart-stories | Storybook stories for the shadcn chart wrapper using recharts demo data/colors. | Update stories to showcase chartTheme recipes (themed line, themed bars, burndown, split bar) so the theme has a living reference. | 3 | small |

### density-glyphs — 30 findings

| # | File | Topic | Current | Target | Phase | Effort |
|---|---|---|---|---|---|---|
| 1 | `webapp/src/components/budget/mobile-budget-list.tsx:33-45, 96-104, 181-207` | budget-risk-group-headers | Mobile Presupuesto groups rows into three risk sections via RiskSection: labels "Sobre límite" (labelClass text-z-expense), "Cerca del límite" (text-z-brass — brass carrying a verdict, guardrail violation), "Dentro del límite" (text-z-income), rendered as SECTION_EYEBROW_CLASS (text-[10px] font-semibold uppercase tracking-[0.18em]) plus a bare count `<span className="text-[10px] text-muted-foreground">{items.length}</span>`. No aggregate delta. Sort: over/near sorted desc by percentUsed, safe asc by percentUsed (not by absolute delta). Healthy group fully expanded (calm rows cost zero taps). | V2 <GroupSummary /> 15px/600: count + aggregate delta ("7 sobre límite · −$486.000" / "N en orden"); state word once per group; sort worst-first by absolute delta; healthy group collapses behind T3 Disclosure "Ver 10 en orden" with a single "Vas bien" chip. | 3 | medium |
| 2 | `webapp/src/components/budget/mobile-budget-list.tsx:209-268` | budget-row-ladder | BudgetCategoryRow: name `truncate text-sm font-medium` (14px), spent `text-xs font-semibold tabular-nums` colored per-row (text-z-debt / text-z-alert / text-foreground), budget evidence `text-[10px] text-muted-foreground` "/ {formatCurrency(budget)}", icon tile size-6 with `${category.color}20` bg. Progress bar: `h-1.5 w-full rounded-full bg-white/5` (6px) with fill `bg-z-debt` (>100), `bg-z-alert` (85-100), `bg-z-income` (healthy — colored, not white/25). | V3 13px row carrying only its own delta in the group's color; V4 11px evidence ("$381.000 de $300.000"); bars 3px hairlines track white/6; only over-limit rows tint debt-red; healthy fills white/25 — never green/brass. | 3 | small |
| 3 | `webapp/src/components/plan/tabs/plan-tab-presupuesto.tsx:149-233` | budget-hero-statechip | Mobile budget hero: percent at `text-[42px] font-[680] leading-none tracking-[-0.06em]` colored text-z-debt/text-z-income, PLUS a second large number `text-lg font-semibold` (totalSpent) beside it. Verdict = <StateChip> uppercase 9px pill with labels {stable:"En control", watch:"Atención", critical:"Sobre límite"} (state-chip.tsx: `text-[9px] font-bold uppercase tracking-[0.06em]` rounded-full 12%/25% tint). Hero bar h-2.5 with gradient `from-z-debt/90 to-z-debt/65` or income equivalent. | One V1 34px/800 number; StateChip → <Verdict /> with closed vocabulary ("Te pasaste · −$486.000", sentence case, never uppercase); hero bar 6px 100% debt when over; spent/budget amounts demote to detail/meta lines. | 1 | medium |
| 4 | `webapp/src/components/budget/budget-category-grid.tsx:134-137 (+ budget-category-card.tsx 39-131)` | budget-desktop-grid | Desktop Presupuesto tab renders `localCategories.map(...)` in incoming order into `grid gap-4 sm:grid-cols-2` — no grouping, no worst-first sort, no summary rows. Each BudgetCategoryCard: bar `h-2 bg-muted` filled with raw category.color (bg-z-debt ≥100, bg-z-alert ≥75), per-card `Badge variant="outline" text-[10px]` "Fijo"/"Variable", evidence "Gastado: X de Y" text-xs, "Disponible:" colored text-z-income/text-z-debt per card — equal-volume wall of ~equal cards. | Ladder + GroupSummary: V2 header per state group, top offenders first, healthy cards behind Disclosure; per-card state color demotes to delta-only; kill per-card Fijo/Variable badge repetition at full volume. | 3 | large |
| 5 | `webapp/src/components/budget/budget-summary-bar.tsx:40-70, 99-137` | budget-summary-bar-desktop | Header row has two `text-lg font-semibold tabular-nums` numbers ("{totalSpent} / {totalBudget}" and "{percent}%" colored text-z-debt >100 / text-z-expense >80) competing; below: stacked per-category color bar (h-2, raw cat.color segments) + wrap-around dot legend (size-2.5 dots per category). | Percent verdict moves to the single hero Verdict; summary becomes V2 (15/600); stacked multicolor bar + legend replaced under chartTheme rules (inline swatch chips, max 2 series). | 3 | medium |
| 6 | `webapp/src/components/mobile/v2/plan/plan-budget-hero.tsx:23-27, 60-114, 128-159` | plan-resumen-second-hero | Plan resumen (mobile) has ANOTHER 46px budget hero: `text-[46px] font-[680] leading-none tracking-[-0.06em]` colored percent + `text-lg font-semibold` spent + StateChip with DIFFERENT vocabulary {stable:"Holgado", watch:"Atención", critical:"Crítico"}. Expanded top-4 category rows each carry a colored percent (`text-z-debt`/`text-z-alert`/`text-z-income` per row) + 1px (h-1) state-colored bars — state repeated per row. | One V1 per screen; closed verdict vocabulary (Vas bien / Cerca del límite / Te pasaste / Atención); expanded rows demote to V3 with delta only, 3px hairline bars. | 1 | medium |
| 7 | `webapp/src/components/mobile/v2/deudas/deudas-hero.tsx:56-88` | deudas-mobile-hero-splitbar | Cuota mensual hero: `text-[32px] font-[680] leading-none tracking-[-0.05em] tabular-nums` (white) + second number `text-[16px] font-semibold text-z-debt` interest. Split bar `flex h-2.5 rounded-full`: capital = `bg-gradient-to-r from-z-white/95 to-z-white/70`, interest = `from-z-debt/80 to-z-debt/50`. Footer "{capitalPct}% capital · {interestPct}% costo financiero" at text-[10px]. No verdict chip. | V1 34/800 white total cuota + Verdict chip "Vas bien · 24% del ingreso"; T2 split bar signature: 10px height radius 999, capital=brass, interés=expense 0.85 opacity, restante=white/10. | 3 | small |
| 8 | `webapp/src/components/mobile/v2/deudas/deudas-cuentas-lens.tsx:476-499, 501-674` | deudas-account-rows-brass | AccountRow: every balance `text-sm font-bold tabular-nums text-z-debt` (all rows red at same volume); TickGauge = 14 segments `h-2 flex-1 rounded-[1.5px]` filled `bg-z-brass` for healthy credit cards (brass on rows — guardrail violation), `bg-z-alert` when usage ≥75, `bg-z-income` for loans, track `bg-white/8`; percent label `text-[10px] font-bold` colored text-z-brass/text-z-alert/text-z-income per row; meta `text-[10px]` "cuota $X · N% EA"; each expanded row exposes its own BRASS_BUTTON ("Pagar tarjeta"/"Abonar") — multiple brass actions per screen. | V2 header "3 cuotas este mes / 2 pagadas"; rows carry deltas only; pending row full opacity + "vence en 2 días" alert chip + owns the ONLY brass action; per-debt capital/interés split = V4 evidence in row; healthy gauge fills white/25 not brass. | 3 | medium |
| 9 | `webapp/src/components/mobile/v2/deudas/deudas-cuentas-lens.tsx:73-104, 466-474` | deudas-group-dividers-no-summary | GroupDivider renders `<span className="h-px flex-1 bg-white/6" /><span className={MOBILE_EYEBROW_CLASS}>Tarjetas de crédito / Préstamos</span>` — hairline eyebrow with NO count and NO aggregate above 2+ same-state rows. | V2 GroupSummary above each group: count + aggregate delta ("N cuotas este mes / N pagadas", "N de M pagados"). | 3 | small |
| 10 | `webapp/src/components/mobile/v2/deudas/deudas-cuentas-lens.tsx:137-187` | deudas-paid-vs-pending | Paid treatment exists only for archived debts: collapsed card "Obligaciones cerradas" + `{archived.length} pagada{s} por completo` (text-[10px]), rows with `text-xs font-bold tabular-nums text-z-income` totalPaid. Active accounts have NO paid/pending distinction this month — no dimming, no income check chips on paid cuotas. | Paid rows dim opacity 0.6 with income check chips inside the same list; V2 "N de M pagados" summary; celebration residue row later (T4). | 3 | medium |
| 11 | `webapp/src/components/debt/debt-hero-card.tsx:35-72 (+ debt-quick-stats.tsx 202-441, debt-account-card.tsx 57-141)` | deudas-desktop-number-wall | Desktop Deudas stacks: hero card with THREE bold numbers (`text-2xl sm:text-3xl font-bold` totalDebt, `text-xl sm:text-2xl font-bold` pagas al mes, `text-2xl font-bold text-z-expense` intereses/mes), then DebtQuickStats with up to NINE `text-xl font-bold` StatTiles + 48px UtilizationRing, then per-account DebtAccountCards each with `text-2xl font-bold` balance + repeated `Badge variant="outline"` "Tarjeta"/"Préstamo" per card + `Progress h-1.5` + text-z-expense interés — a full number wall, no single decider. | Exactly ONE V1 (34/800, derived judgment) + verdict slot; tiles demote to V2/V3; per-debt capital/interés = V4 evidence inside rows; kill repeated type badges. | 3 | large |
| 12 | `webapp/src/components/debt/debt-account-row.tsx:70-80` | debt-account-row-brass-fill | Canonical compact debt row's utilization bar: `h-1.5 bg-white/6` with fill `utilization > 60 ? "bg-z-debt/80" : "bg-z-brass/80"` — brass fill on healthy bars. | 3px hairline; healthy fill white/25 (NOT brass); only over-limit tints debt-red. | 3 | trivial |
| 13 | `webapp/src/components/recurring/recurring-template-card.tsx:148, 195-201` | state-once-recurring-card | Every recurring template card renders `<StatChip label="Estado" value={occurrenceStatus === "paid" ? "Pagado ✓" : "Pendiente"} />` (StatChip label is `text-[8px] font-semibold uppercase tracking-[0.18em]`) — state repeated per row across the whole list. | STATE ONCE: verdict word only in the V2 group summary ("N de M pagados"); rows carry date/amount deltas only; paid rows dim with income check. | 3 | small |
| 14 | `webapp/src/components/recurring/recurring-timeline.tsx:45-61, 153-156, 188-191, 219-224` | state-once-recurring-timeline | Desktop timeline stamps every date group with an uppercase colored status label: `text-[10px] font-semibold uppercase tracking-[0.18em]` + dateColor (text-z-debt overdue / text-z-alert today / text-z-income paid) and prefix strings "Vencido — " / "Hoy — "; separate uppercase headers "Completados" (text-z-income) and "Pausados" (text-muted-foreground/50) — same-state groups repeat the alarm treatment per group. | One V2 summary per state; per-row/per-group uppercase alarm labels deleted; relative dates per T5 ladder. | 3 | medium |
| 15 | `webapp/src/components/mobile/v2/plan/mobile-recurrentes-view.tsx:52-74, 244-263, 285-308, 527-547` | state-once-recurrentes-mobile | Mobile Recurrentes: hero `text-3xl font-bold` compromiso + two colored counts `text-lg font-semibold text-z-alert` (Pendientes) and `text-z-income` (Completados) — 3 competing numbers; each pending date-group gets uppercase colored header (`text-[10px] font-semibold uppercase tracking-[0.18em]` + statusLabel debt/alert) AND statusAccent `border-l-2 border-l-z-debt bg-z-debt/5` per group. CompletedSection header "Completados ({N})" with "Ocultar ↑ / Ver ↓" toggle; completed rows `opacity-60` + `Check text-z-income` (closest existing match to target paid-row treatment). | One V1 + verdict slot (Deudas/Recurrentes adopt full hero slot per T1); V2 "N de M pagados" summaries; state once per group; disclosure label grammar "Ver/Ocultar + qué". | 3 | medium |
| 16 | `webapp/src/components/mobile/v2/movimientos/movimientos-transaction-row.tsx:88-128` | state-once-movimientos-rows | Every transaction row carries TWO pills: category chip (`rounded-full border px-2 py-px text-[11px] font-medium` with per-category color-mix bg/border) + account pill (`rounded-full bg-white/5 px-2 py-0.5 text-[10px]` containing a 5px colored dot `h-[5px] w-[5px] rounded-full` styled with tx.account.color + accountTail(name)). Amount `text-[15px] font-medium tabular-nums` colored text-z-income/text-z-expense per row. Date group headers (movimientos-root.tsx 215-222) are bare eyebrow + hairline, no count/aggregate. | Rows demote to V3 13px with compact meta (dot stays — it IS the standard glyph); date groups gain V2 count+aggregate summaries where 3+ same-state rows repeat. | 3 | medium |
| 17 | `webapp/src/components/transactions/transaction-table.tsx:287-305` | state-once-transaction-table-desktop | Desktop table stamps every row with `<Badge variant={tx.status === "POSTED" ? "secondary" : "outline"}>` "Confirmada"/"Pendiente"/"Cancelada" — nearly all rows are POSTED, so an entire column repeats the same state pill. | STATE ONCE: status only when it deviates (Pendiente/Cancelada); confirmed is the silent default. | 3 | small |
| 18 | `webapp/src/components/tendencias/delta-chip.tsx:1-14 (+ category-trend-list.tsx 82-89)` | state-once-tendencias-delta-chips | Every category row in Tendencias renders a DeltaChip pill: `rounded-full px-1.5 py-0.5 text-[11px] font-semibold tabular-nums` with `bg-z-expense/12 text-z-expense` (up) / `bg-z-income/10 text-z-income` (down) and glyphs "▲/▼/~" — a per-row colored pill wall. | Rows carry deltas only in the group's color (V3, no pill chrome per row); summary-first if 3+ rows share a state. | 3 | small |
| 19 | `webapp/src/components/budget/trend-comparison.tsx:42-55` | state-once-budget-trend-badges | Per-category anomaly Badge repeated per row: `Badge variant="outline" text-[10px] px-1.5` with `border-z-debt/30 text-z-debt` or `border-z-income/30 text-z-income` and text "+N% vs promedio". | Delta text inline at V3/V4 volume in the group color; no per-row outlined pills. | 3 | small |
| 20 | `webapp/src/components/mobile/v2/inicio/inicio-attention-timeline.tsx:66-101` | state-once-inicio-attention-cards | "Por resolver" strip: each card repeats an uppercase urgency label `text-[9px] font-semibold uppercase tracking-[0.18em]` colored text-z-debt (overdue) / text-z-brass (today — brass as state) / text-muted-foreground, plus per-card tinted borders `border-z-debt/30 bg-z-debt/5` / `border-z-brass/30`. | Urgency stated once (group summary or Verdict atención); cards carry only date + amount; brass never signals state. | 3 | small |
| 21 | `webapp/src/components/mobile/v2/deudas/deudas-cuentas-lens.tsx:242-267 (+ 366-374; mobile-recurrentes-view.tsx 527-534; recurring-summary-bar.tsx 20-33; movimientos-lectura.tsx 264-291; mobile-budget-list.tsx 106-155; category-trend-list.tsx 297-306)` | group-summary-prior-art | Existing count+aggregate headers to converge into <GroupSummary />: PersonasCard "{N} activa(s)" + te deben/debes totals (closest V2 prototype); HeaderTiles "Deuda total" + "{N} cuenta(s)"; CompletedSection "Completados ({N})" + Ver/Ocultar toggle; RecurringSummaryBar "{total} en fijos · {N} pendientes · {N} completados"; Movimientos "Resumen del mes" (count text-[18px] + 2 aggregates text-[15px]); "Sin límite" + count with chevron collapse; "Ver todas ({N})". | All converge on V2 GroupSummary grammar: "N sobre límite" / "N en orden" / "N de M pagados" at 15px/600, count + aggregate delta, healthy groups behind Disclosure. | 3 | medium |
| 22 | `webapp/src/components/debt/bank-badge.tsx:23-93` | glyph-system-a-letter-badge | BankBadge = the letter-badge system the handoff DELETES: BRANDS regex table (bancolombia #FDDA24, nu #820AD1, davivienda #ED1C27, nequi #200020, falabella #007A33, bogotá #002B7F, lulo #E2FF32, confiar #00843D, popular #FFD100, bbva #072146, scotiabank #EC111A) rendering `size-8 rounded-lg border border-white/6 text-[13px] font-bold` monogram with brand bg/fg (or /banks/<key>.png img). Call sites: deudas-cuentas-lens.tsx:164 (`className="size-7 text-[11px]"`) and :543. | Deleted — replaced by the colored account dot everywhere (the smaller, quieter system). | 3 | small |
| 23 | `webapp/src/components/accounts/account-icon.tsx:15-65 (+ account-row-identity.tsx 49-94)` | glyph-system-b-icon-tile | AccountIcon = second glyph system: BANK_LOGOS[bank_key] SVG (lib/icons/bank-logos.tsx) or lucide TYPE_GLYPHS (Wallet/PiggyBank/CreditCard/Landmark/Banknote/TrendingUp) inside a tinted tile `inline-flex items-center justify-center rounded-md` size-5/size-7 with `backgroundColor: ${color}1a`. Consumed via AccountRowIdentity (densities compact/picker/detail) at: inicio-activity.tsx:184, mobile-recurrentes-view.tsx:338, mobile-recurrentes-templates-strip.tsx:84, occurrence-actions.tsx:318, confirm-income-dialog.tsx:191, pay-expense-dialog.tsx:363, plan-main-accounts-section.tsx:50. | Standardize on colored dot; AccountRowIdentity is the single choke point — swap its AccountIcon render for the dot and all 7 call sites migrate at once. | 3 | medium |
| 24 | `webapp/src/components/mobile/v2/movimientos/movimientos-transaction-row.tsx:121-127` | glyph-system-c-colored-dot | The colored-dot system (handoff TARGET) already live at 5 call sites with 3 divergent sizes: movimientos row `h-[5px] w-[5px] rounded-full` + backgroundColor tx.account.color inside a `bg-white/5 px-2 py-0.5 text-[10px]` pill with accountTail(name); transaction-table.tsx:195-198 `h-1.5 w-1.5 rounded-full align-middle mr-1`; :274-277 `h-2 w-2 rounded-full shrink-0`; inbox-transaction-row.tsx:306-311 and auto-review-row.tsx:69-73 `h-1.5 w-1.5`; mobile-presupuesto.tsx:236-242 `size-2`. | One canonical AccountDot component (single size/recipe) used everywhere; existing call sites converge on it. | 3 | small |
| 25 | `webapp/src/components/debt/debt-account-card.tsx:36-44` | glyph-hardcoded-fallback-hue | Desktop debt card icon tile hardcodes an indigo fallback: `backgroundColor: ${account.color ?? "#6366f1"}20` and `color: account.color ?? "#6366f1"` — a hue that exists nowhere in the token set. AccountCard (accounts/account-card.tsx:57-62) uses a different fallback pair (var(--z-brass) / var(--z-olive-deep)) with a size-10 rounded-xl tile. | Colored dot with token-based fallback; zero new hues. | 3 | trivial |
| 26 | `webapp/src/components/dashboard/hybrid-hero.tsx:126-175 (+ interactive-metric-card.tsx:222, burn-rate-card.tsx:117, health-meter-expanded.tsx:155, dashboard-hero.tsx:96)` | one-v1-dashboard-desktop | Desktop Inicio: HybridHero `text-[44px] font-bold tracking-[-0.04em] tabular-nums` (Gasto de hoy, tone-colored) + status pill (uppercase `text-[10px] tracking-[0.12em]` + size-1.5 dot — the "dashboard status dot" T1 deletes) COMPETES with interactive-metric-card `text-4xl font-bold`, burn-rate-card `text-3xl font-bold`, health-meter-expanded `text-4xl font-extrabold`, and legacy dashboard-hero `text-4xl md:text-5xl` — 4-5 hero-scale numbers on one screen. | ONE V1 34/800 (the derived ¿voy bien? number); all others demote to V2/V3; dot+sentence converges into <Verdict /> (sentence survives as detail line). | 3 | large |
| 27 | `webapp/src/components/mobile/v2/inicio/pulse-widget.tsx:193-216 (+ inicio-burndown.tsx:240, widgets/where-today-widget.tsx:30, widgets/attention-widget.tsx:56)` | one-v1-inicio-mobile | Mobile Inicio: pulse-widget `text-[36px] font-extrabold` /día + inicio-burndown `text-[24px] font-[680]` + where-today-widget `text-[26px] font-bold` + attention-widget `text-[26px] font-bold` — four 24px+ numbers competing in one scroll. | One V1; widget numbers cap at V2 15/600. | 3 | medium |
| 28 | `webapp/src/components/plan/plan-hero.tsx:62-67 (+ plan-scenario-preview.tsx:30; mobile: plan-budget-hero.tsx:68, plan-net-hero.tsx:60)` | one-v1-plan-resumen | Plan resumen desktop: plan-hero `text-4xl md:text-5xl font-bold` disponible (colored text-z-debt when negative) + plan-scenario-preview `text-3xl` count. Mobile plan root stacks plan-budget-hero 46px AND plan-net-hero `text-3xl font-bold` — two 30px+ numbers. | One V1 per screen (the disponible verdict); scenario count and secondary heroes demote to V2. | 3 | medium |
| 29 | `webapp/src/components/debt/debt-free-countdown.tsx:36-39 (+ deudas-hero.tsx:58, mobile-recurrentes-view.tsx:248-263)` | one-v1-deudas-lenses | DebtFreeCountdown `text-[42px] font-extrabold` months + `text-2xl font-bold` "meses" suffix (Deudas Plan lens); Carga lens hero is 32px — per-lens it's roughly one hero (compliant-ish), but Recurrentes hero has `text-3xl` + two `text-lg` colored counts, and transactions desktop page (app/(dashboard)/transactions/page.tsx:157,176) shows TWO `text-3xl font-semibold` stat numbers side by side. | Normalize all heroes to V1 34/800 spec (letter-spacing -0.02em tabular); side counts become V2 group summaries. | 3 | medium |
| 30 | `webapp/src/lib/constants/styles.ts:59-95` | eyebrow-vs-v2-ladder-gap | The codebase has ONE group-header primitive: SECTION_EYEBROW_CLASS = "text-[10px] font-semibold uppercase tracking-[0.18em] text-z-sage-dark" (MOBILE_EYEBROW_CLASS aliases it). There is NO 15px/600 V2 summary primitive and no 34/800 V1 constant — every hero hand-rolls its size (42px/44px/46px/32px/36px with font-[680]/bold/extrabold variants). | Add ladder constants (V1 34/800 ls -0.02em tabular, V2 15/600, V3 13, V4 11) + <GroupSummary /> component in Fase 3; heroes converge on V1 constant. | 3 | small |

### celebrations — 17 findings

| # | File | Topic | Current | Target | Phase | Effort |
|---|---|---|---|---|---|---|
| 1 | `webapp/src/app/globals.css:256-262` | keyframes-insertion-point | The ONLY custom keyframe in the app: `@keyframes page-enter { from { opacity: 0; transform: translateY(3px); } to { opacity: 1; transform: translateY(0); } }` + `.animate-page-enter { animation: page-enter 0.1s ease-out; }`. Line 2 imports `tw-animate-css` (its `animate-in fade-in` etc. utilities are used in ~18 call-sites). NO `--motion-*` tokens exist anywhere in globals.css (T3 adds them). The verdict-chip tint recipe already exists at lines 243-247: `.surface-income/.surface-expense/.surface-debt/.surface-alert` using `color-mix(in srgb, var(--tok) 8%, transparent)` bg + 20% border. | Add `z-ring-sweep` (dashoffset 100→0, 600ms — single exception to 200ms cap) and `z-rise` (opacity 0→1 + translateY 6px→0, 120ms) keyframes next to page-enter; both consume T3 `--motion-ease` token. | 4 | trivial |
| 2 | `webapp/src:-` | no-celebration-infra | Zero celebration infrastructure: grep for confetti/celebrat/felicidades/logro across webapp/src returns only comments (onboarding page.tsx:65 '"Listo" celebration', debt-free-countdown.tsx:11 'Celebratory state', hybrid-hero.tsx:84 'no achievement to celebrate in green'). No confetti library, no <Celebration> component, no brass-on-number treatment anywhere. ~105 `toast.success` call-sites all mutation-scale (e.g. 'Obligación archivada — el historial se conserva', 'Movido a pendientes'), none celebration-scale. | Net-new celebration primitive: brass hero number + eyebrow (DEUDA SALDADA / MES CERRADO BAJO PLAN / TODO PAGADO) + T1 'Vas bien' chip + z-ring-sweep/z-rise motion, event-gated, once per event, max one per screen visit. | 4 | large |
| 3 | `webapp/src/components/dashboard/debt-free-banner.tsx:9-34` | debt-free-banner-app-open-trigger | `DebtFreeBanner` renders on EVERY desktop dashboard visit (mounted at zones/hero-zone.tsx:85): income-tinted banner `Libre de deudas: ${displayDate} (${data.monthsToFree} meses)` with inline styles `color-mix(in srgb, var(--z-income) 8%, transparent)` bg / 20% border, `animate-in fade-in duration-150`. It is a projection (countdown), not an earned event — a repeated app-open 'success' surface. | Not a celebration under T4 rules (never app-open). Stays as informational line; the earned moment is Event 1 (deuda saldada) fired once when a debt actually reaches zero. | 4 | small |
| 4 | `webapp/src/components/debt/debt-free-countdown.tsx:10-24` | debt-free-countdown-celebratory-state | State-based celebratory card when user has no debts: `<p className="text-sm font-semibold text-z-income">¡Libre de deudas!</p>` + 'No tienes cuentas de deuda activas.' inside `rounded-xl bg-z-surface-2 border border-z-border` with CheckCircle2 in `bg-z-income/15` circle. Renders every visit (app-open trigger) and uses exclamation marks — violates copy rule 4 (tuteo, past tense, no exclamation). NOTE: only mounted via `PatrimonioSection` (patrimonio-section.tsx:45) which has no importers — effectively dead code, but the string survives as migration-relevant residue. | Copy re-cased to closed vocabulary, no exclamation; the one-time payoff moment moves to Event 1 (ring sweep + '$0' brass 30px/800 + detail 'Pagaste $X en N meses…' + brass-ghost 'Ver historial'). | 4 | small |
| 5 | `webapp/src/components/accounts/quick-actions-bar.tsx:190-201, 275-280, 323-344` | debt-zero-lifecycle-manual-archive | What happens when a debt hits zero TODAY: nothing automatic. The account stays listed (deudas-cuentas-lens.tsx:568-580 flips the amount to `text-z-income` when `account.balance > 0` is false; several aggregates filter it out via `.filter((a) => a.balance > 0)` — deudas/page.tsx:92,195, deudas-lens-root.tsx:84,136). Removal is a MANUAL dropdown action 'Archivar (pagada)' (shown for any debt regardless of balance) → confirm Dialog 'Archivar obligación / Marca esta obligación como pagada por completo…' → `archiveDebtObligation` sets `is_active: false` (actions/accounts.ts:295-319) → `router.push("/accounts")` + `toast.success("Obligación archivada — el historial se conserva")`. No celebration anywhere in the path. | Event 1 'Deuda saldada': one-time moment on zero-balance (ring sweeps closed 600ms, '$0' brass, text rise delayed 450ms, detail sentence, CTA 'Ver historial'); row moves to 'Saldadas' group at opacity 0.7 instead of vanishing to /accounts. | 4 | large |
| 6 | `webapp/src/components/mobile/v2/deudas/deudas-cuentas-lens.tsx:131-187` | saldadas-group-partial | A Saldadas-like group EXISTS on mobile Cuentas lens only: `ClosedObligations` collapsible — header 'Obligaciones cerradas' + `{archived.length} pagada{s} por completo` (Archive icon `text-z-income`), rows show name, 'cerrada el {formatDate(...)}', `totalPaid` in `text-xs font-bold tabular-nums text-z-income` + 'pagado en total'. Fed by `getArchivedDebtObligations()` (actions/debt.ts:457-527: accounts with `is_active=false` + type CREDIT_CARD/LOAN, lifetime INFLOW sum), streamed via Suspense (line 107-111). Desktop Deudas page has NO equivalent — archived debts simply disappear from `getDebtOverview`. | Rename/align to 'Saldadas' group semantics (rows opacity 0.7, income check chips per T6 Deudas application); add the desktop counterpart; group receives the debt row at payoff instead of the row vanishing. | 4 | medium |
| 7 | `webapp/src/components/mobile/v2/inicio/inicio-activity.tsx:136-259` | actividad-reciente-mobile-row-shape | Live mobile 'recent activity': eyebrow `Reciente` (SECTION_EYEBROW_CLASS, line 138), 3 visible rows (line 117 `transactions.slice(0, 3)`). Row anatomy: 22px icon square (`bg-z-income/12 text-z-income` / `bg-z-expense/12 text-z-expense`, ArrowDownLeft/ArrowUpRight), description `text-xs font-medium` (+Repeat icon `text-z-brass/70` if recurring), meta line `text-[10px] text-muted-foreground` (AccountRowIdentity + CategoryIcon), amount `text-xs font-medium tabular-nums` (+`text-z-income` for INFLOW), footer link 'Ver todos' `text-z-brass`. Rows expand to TransactionQuickActions via grid-rows 0fr→1fr duration-200. Mounted as widget detail through `renderRecentWidget` (widgets/recent-widget.tsx:53-117, chip preview eyebrow 'Reciente', empty states 'Sin movimientos aún' / 'Aún no hay movimientos que mostrar aquí.'). | Insertion point for 'logro' residue rows ('Logro / junio cerró bajo plan · margen $412.000', deuda saldada, todo pagado). Requires a row-type discriminator — `RecentActivityTx` (recent-widget.tsx:20-47) is transaction-only with no `type` field, so residue rows need a union type or a dedicated logro row component rendered above tx rows. | 4 | medium |
| 8 | `webapp/src/app/(dashboard)/dashboard/page.tsx:247-306` | actividad-reciente-desktop | Desktop equivalent is the 'Últimas transacciones' Card (WidgetSlot `widgetId="recent-tx"`): CardTitle 'Últimas transacciones', 'Ver todas' link, rows are PrefetchLink to `/transactions/{id}` with ArrowDownLeft/ArrowUpRight `text-z-income`/`text-z-expense` icons, merchant + formatDate, amount `text-sm font-medium` (+`text-z-income` INFLOW). Empty: 'No hay transacciones aún. Registrar una'. Fed by `getRecentTransactions()`. | Same residue-row insertion on desktop; a logro row appears alongside transaction rows after each celebration event. | 4 | small |
| 9 | `webapp/src/components/mobile/dashboard/../mobile-dashboard-v2.tsx:100` | actividad-reciente-dead-heading | webapp/src/components/mobile/mobile-dashboard-v2.tsx:100 contains the literal heading 'Actividad reciente' but the component has zero importers (replaced by InicioRoot + widgets). Dead code — do not migrate it; flag for deletion. | None (delete candidate). The handoff's 'Actividad reciente' maps to InicioActivity/recent-widget, not this file. | 4 | trivial |
| 10 | `webapp/src/components/mobile/v2/plan/mobile-recurrentes-view.tsx:244-263` | recurrentes-paid-count-grammar | Mobile Recurrentes hero (HERO_CARD_GRADIENT_CLASS card): eyebrow 'Compromiso mensual' `text-z-brass`, number `text-3xl font-bold tabular-nums`, then two SEPARATE count chips — 'Pendientes' `text-z-alert` + `{hook.pending.length}` and 'Completados' `text-z-income` + `{hook.completed.length}` (lines 253-263). Desktop: RecurringSummaryBar (recurring-summary-bar.tsx:20-33) '`{totalPlanned}` en fijos · N pendiente(s) · M completado(s)'; RecurringCompletedSection trigger (recurring-completed-section.tsx:33-41) '{completed.length} pago(s) completado(s) este mes' in `border-z-income/20 bg-z-income/8 text-z-income`. Nowhere uses the 'N de M pagados' grammar (copy rule 6). | Converge to 'N de M pagados' count display; this count is what turns brass 34/800 in Event 3 ('11 de 11 pagados'). | 4 | medium |
| 11 | `webapp/src/components/mobile/v2/plan/mobile-recurrentes-view.tsx:385-395` | recurrentes-all-paid-state | All-paid state renders EVERY visit when `sortedDates.length === 0 && hook.completed.length > 0`: PANEL_INSET_CLASS card with `<Check className="mx-auto size-6 text-z-income" />`, 'Todo al día' `text-xs font-medium text-z-income`, '{N} pago(s) completado(s)' `text-[10px] text-muted-foreground`. State-based (app-open), green not brass, no eyebrow, no segment strip. Sibling empty state (379-383): 'No hay pagos recurrentes este mes'. CompletedSection below (501-534): 'Completados (N)' + toggle 'Ocultar ↑'/'Ver ↓' (bare 'Ver' — also a T3 violation), rows `opacity-60` with income Check. | Event 3 'Todo pagado': fires ONCE when the LAST pending recurrente clears — count 'N de N pagados' turns brass 34/800 (200ms), eyebrow 'Todo pagado' brass, T1 chip flips Atención→Vas bien, segment dot strip (flex-1 h-4 radius 2, paid=brass unpaid=white/10), residue row in Actividad reciente. The always-on 'Todo al día' card becomes the quiet steady-state. | 4 | large |
| 12 | `webapp/src/components/mobile/v2/inicio/inicio-attention-timeline.tsx:40-63` | por-resolver-inbox-empty-state | The 'Por resolver' inbox exists as the attention timeline (eyebrow 'Por resolver', 'Ver todo →' to /gestionar). Empty state (56-63): CheckCircle2 `text-z-income` + 'Todo tranquilo' + 'Sin pendientes esta semana.' Companion widget chip (widgets/attention-widget.tsx:45-72): eyebrow 'Por resolver', empty = CheckCircle2 + 'Al día', a11y label 'Sin pendientes'. Full-page hub empty state (gestionar/attention-hub.tsx:13-26): CheckCircle2 size-10 `text-z-olive-deep` + 'Al día' + 'Ir al dashboard →'. Desktop AttentionCard empty (ui/attention-card.tsx:55-61): eyebrow 'Estado' + 'Al día' in `text-z-olive-deep`. None mention the next upcoming charge. | Event 3 empty-inbox copy: 'Nada por resolver. El próximo cobro es Netflix, el 2 de agosto.' — needs next-pending-occurrence lookup (available from getPendingOccurrences / upcomingPayments already fed into this component). | 4 | medium |
| 13 | `webapp/src/components/dashboard/dashboard-alerts.tsx:31-62` | month-close-detection-netnew | NO month-close detection exists anywhere (grep 'mes cerrado', 'cierre de mes', 'month_closed', 'monthClose' → zero product hits). Event 2 'first visit after month close' is net-new state. Two existing persistence precedents to reuse: (1) localStorage snooze map in dashboard-alerts.tsx — `STORAGE_KEY = "zeta_dismissed_alerts"`, `getDismissedMap()/dismissAlert()/isDismissed()` with 7-day SNOOZE_MS; (2) server-side nested JSONB state in `profiles.dashboard_config` — guided experience stores `firstSteps.dismissedAt` there (actions/guided-experience.ts:89-186, read/write via actions/dashboard-config.ts:29-89 with `updateTag("dashboard-config")`). A `lastCelebratedMonth` key fits pattern (2) for cross-device once-only, or pattern (1) for cheap per-device. | First-visit-after-month-close flag (profile JSONB `celebrations.lastClosedMonthShown: "2026-06"` or localStorage) gating Event 2; fires once, never on subsequent visits. | 4 | medium |
| 14 | `webapp/src/actions/charts.ts:39-45` | month-close-margin-sources | Margin ('mes cerrado bajo plan · margen $X') is computable from existing cached queries — nothing new needed: (1) `MonthlyCashflow { month, label, income, expenses, net }` via `getMonthlyCashflow(month, currency)` (charts.ts:560-564, cached `getMonthlyCashflowCached` line 153) — `net` of the just-closed month = cash margin; (2) `getBudgetSummary(month)` → `{ totalTarget, totalSpent, progress }` (actions/budgets.ts:81-92) — `totalTarget - totalSpent` = under-plan margin and `progress` feeds the chip '% del plan'. Both accept a month param so the previous month can be queried on first visit of the new month. | Event 2 hero: unspent margin brass 34/800 + 'sin gastar', chip 'Vas bien · 79% del plan', CTAs 'Mover al ahorro' (brass) + 'Ver julio' (ghost), residue row 'Logro / junio cerró bajo plan · margen $412.000'. | 4 | medium |
| 15 | `webapp/src/components/mobile/v2/deudas/deudas-cuentas-lens.tsx:548-560` | ring-gauge-dependency | No radial ring exists in product code for Event 1 to sweep: debt progress renders as linear elements only — `TickGauge` bar (deudas-cuentas-lens.tsx:550), horizontal bars in debt-free-countdown.tsx:51-59 (`bg-z-income` fill) and deudas-plan-lens.tsx:73-78 (`bg-z-brass/80` fill, '{pct}% del camino recorrido'). All strokeDasharray hits are recharts chart internals, not gauges. T4's Event 1 ring (pathLength=100 + z-ring-sweep on dashoffset) depends on the T2 Tier-1 SVG ring landing first — matches the spec note 'Ships last: depends on Verdict, ring, motion tokens'. | Event 1 reuses the T2 270° ring (viewBox 0 0 128 128, pathLength=100) with `animation: z-ring-sweep 600ms var(--motion-ease)` sweeping to full on payoff. | 4 | medium |
| 16 | `webapp/src/components/import/step-results.tsx:59-83` | existing-event-success-screens | Two existing event-gated (correctly one-time) success surfaces to keep stylistically consistent with T4: (1) import results hero — CheckCircle2 `h-16 w-16 text-z-income` + headline '{N} movimientos importados' / 'Ya tenías todo esto' / sub 'Tu historial quedó al día.'; (2) onboarding step 4 (app/onboarding/page.tsx:467-499) — CheckCircle2 size-9 in `bg-z-income/10 text-z-income` circle, 'Listo, {firstName}.', `animate-in fade-in slide-in-from-right-4 duration-200`, title constant '¡Listo!' at line 74 (exclamation violates copy rule 4). Neither uses brass-on-number. | Leave as-is functionally; align copy (drop '¡Listo!' exclamation) and motion to T3/T4 vocabulary when touched. They are precedent, not migration targets. | 4 | trivial |
| 17 | `webapp/src/lib/constants/styles.ts:59, 71, 95` | celebration-eyebrow-slot | Eyebrow + hero-card primitives the celebration reuses already exist: `SECTION_EYEBROW_CLASS` (=`MOBILE_EYEBROW_CLASS`, 10px uppercase tracking voice used by 'Reciente', 'Por resolver', 'Pendientes', 'Compromiso mensual', 'Libre de deudas') and `HERO_CARD_GRADIENT_CLASS` (used by the recurrentes hero card). Celebration eyebrows 'DEUDA SALDADA / MES CERRADO BAJO PLAN / TODO PAGADO' would be these same classes with brass color override, per 'same 10px uppercase voice'. | Eyebrow swaps to the celebration string in brass during the moment; hero number class gains a brass state (currently number colors are fixed: text-foreground / tone classes). | 4 | small |

