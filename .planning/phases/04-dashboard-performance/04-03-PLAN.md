---
phase: 04-dashboard-performance
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - webapp/package.json
  - webapp/src/components/ui/chart.tsx
  - webapp/src/components/charts/category-donut.tsx
  - webapp/src/components/charts/cash-flow-view-toggle.tsx
  - webapp/src/components/charts/sparkline.tsx
  - webapp/src/components/charts/net-worth-history-chart.tsx
  - webapp/src/components/charts/budget-pace-chart.tsx
  - webapp/src/components/charts/balance-history-chart.tsx
  - webapp/src/components/charts/daily-spending-chart.tsx
  - webapp/src/components/charts/enhanced-cashflow-chart.tsx
  - webapp/src/components/charts/monthly-cashflow-chart.tsx
  - webapp/src/components/charts/income-vs-expenses-chart.tsx
  - webapp/src/components/dashboard/burn-rate-card.tsx
  - webapp/src/components/debt/utilization-gauge.tsx
  - webapp/src/components/debt/salary-timeline-chart.tsx
  - webapp/src/components/debt/debt-simulator.tsx
  - webapp/src/components/debt/planner/compare-step.tsx
  - webapp/src/components/debt/planner/detail-step.tsx
autonomous: true
requirements: [PERF-04]
must_haves:
  truths:
    - "recharts version is 3.x in package.json"
    - "pnpm build passes clean after recharts v3 upgrade"
    - "Chart color tokens use var(--chart-N) format, not hsl(var(--chart-N))"
  artifacts:
    - path: "webapp/package.json"
      provides: "recharts@3.x dependency"
      contains: "recharts"
    - path: "webapp/src/components/ui/chart.tsx"
      provides: "shadcn chart wrapper compatible with recharts v3"
  key_links:
    - from: "Chart components"
      to: "recharts@3"
      via: "import from recharts"
      pattern: "from \"recharts\""
---

<objective>
Upgrade recharts from v2.15.4 to v3 and fix any TypeScript type errors from the v3 migration. Audit chart color tokens for correct CSS variable format.

Purpose: Recharts v3 brings smaller bundle size and modern API. Upgrading now (before lazy-loading in Plan 04) means Plan 04 only deals with dynamic imports, not type fixes.

Output: recharts@3.x in package.json, all chart files compile clean, color format verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-performance/04-CONTEXT.md
@.planning/phases/04-dashboard-performance/04-RESEARCH.md
@.planning/phases/04-dashboard-performance/04-UI-SPEC.md
@.planning/phases/04-dashboard-performance/04-01-SUMMARY.md

@webapp/package.json
@webapp/src/components/ui/chart.tsx
@webapp/src/components/debt/scenario-planner.tsx

<interfaces>
<!-- Chart files that export NAMED exports (not default) -->
CategoryDonut: export function CategoryDonut({ ... })
BudgetPaceChart: export function BudgetPaceChart({ ... })
NetWorthHistoryChart: export function NetWorthHistoryChart({ ... })
CashFlowViewToggle: export function CashFlowViewToggle({ ... })
Sparkline: export function Sparkline({ ... })
BurnRateCard: export function BurnRateCard({ ... })
BalanceHistoryChart: export function BalanceHistoryChart({ ... })
UtilizationGauge: export function UtilizationGauge({ ... })
SalaryTimelineChart: export function SalaryTimelineChart({ ... })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade recharts to v3 and fix type errors</name>
  <files>webapp/package.json, webapp/src/components/ui/chart.tsx, webapp/src/components/charts/category-donut.tsx, webapp/src/components/charts/cash-flow-view-toggle.tsx, webapp/src/components/charts/sparkline.tsx, webapp/src/components/charts/net-worth-history-chart.tsx, webapp/src/components/charts/budget-pace-chart.tsx, webapp/src/components/charts/balance-history-chart.tsx, webapp/src/components/charts/daily-spending-chart.tsx, webapp/src/components/charts/enhanced-cashflow-chart.tsx, webapp/src/components/charts/monthly-cashflow-chart.tsx, webapp/src/components/charts/income-vs-expenses-chart.tsx, webapp/src/components/dashboard/burn-rate-card.tsx, webapp/src/components/debt/utilization-gauge.tsx, webapp/src/components/debt/salary-timeline-chart.tsx, webapp/src/components/debt/debt-simulator.tsx, webapp/src/components/debt/planner/compare-step.tsx, webapp/src/components/debt/planner/detail-step.tsx</files>
  <read_first>
    webapp/package.json
    webapp/src/components/ui/chart.tsx
    .planning/phases/04-dashboard-performance/04-RESEARCH.md
  </read_first>
  <action>
**Per D-09, D-10, D-11:** Upgrade recharts and fix v3 breaking changes.

### Step 1: Upgrade recharts

```bash
cd webapp && pnpm add recharts@3
```

### Step 2: Run build to discover type errors

```bash
cd webapp && pnpm build 2>&1 | grep -i "error" | head -30
```

### Step 3: Fix type errors based on build output

**Known v3 breaking changes that may affect this codebase (from RESEARCH.md):**

1. **`accessibilityLayer` now defaults to true**: The 9 files that explicitly set `accessibilityLayer` do NOT need changes (harmless redundancy). Optionally remove the explicit prop for cleanliness.

2. **`TooltipProps` renamed to `TooltipContentProps`**: If any chart component types a custom tooltip using `TooltipProps` from recharts, change it to use the correct recharts v3 type or use inline prop types.

3. **`chart.tsx` (shadcn wrapper)**: Read `webapp/src/components/ui/chart.tsx` fully. It imports `* as RechartsPrimitive from "recharts"` and uses `RechartsPrimitive.Tooltip`, `RechartsPrimitive.Legend`, etc. If v3 changes the types on these, fix them. The most likely issue is `Tooltip` content/formatter prop types.

4. **Color format**: CONFIRMED NO CHANGE NEEDED — the codebase already uses `var(--chart-N)` format (not `hsl(var(--chart-N))`). D-10 is satisfied as-is. Verify by grepping: `grep -r "hsl(var(--chart" webapp/src/` should return nothing.

5. **`ResponsiveContainer`**: Check `sparkline.tsx` which uses `ResponsiveContainer`. In v3, the `ref.current.current` double-ref pattern was removed. If sparkline uses refs on ResponsiveContainer, update accordingly. If it just uses `<ResponsiveContainer width="100%" height={32}>`, no change needed.

### Step 4: Iterate until build passes

Run `pnpm build` after each fix. Continue until clean. Do NOT use `as any` casts — fix the actual types per RESEARCH.md "Don't Hand-Roll" guidance.

### Step 5: Verify color format audit (D-10)

```bash
grep -r "hsl(var(--chart" webapp/src/
```
Must return nothing. If any file uses `hsl(var(--chart-N))`, replace with `var(--chart-N)`.
  </action>
  <verify>
    <automated>cd webapp && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/package.json` contains `recharts` with version starting with `3.` (e.g., `"recharts": "^3.` or `"recharts": "3.`)
    - `pnpm build` passes clean (exit code 0) after upgrade
    - `grep -r "hsl(var(--chart" webapp/src/` returns zero results
    - No `as any` casts added to fix recharts type errors
    - `chart.tsx` compiles without TypeScript errors
  </acceptance_criteria>
  <done>recharts upgraded to v3, all TypeScript type errors resolved, build passes clean, color format verified correct</done>
</task>

</tasks>

<verification>
1. `cd webapp && pnpm build` passes clean
2. `grep -r '"recharts"' webapp/package.json` shows version `3.x`
3. `grep -r "hsl(var(--chart" webapp/src/` returns zero results
4. No `as any` casts in chart files
</verification>

<success_criteria>
- recharts v3 in package.json, all chart files compile clean
- Color tokens use `var(--chart-N)` format (not `hsl(var(...))`)
- No `as any` casts added to fix type errors
- pnpm build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-performance/04-03-SUMMARY.md`
</output>
</content>
</invoke>