---
phase: 04-dashboard-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - webapp/src/app/onboarding/page.tsx
  - webapp/package.json
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "framer-motion is not in the project dependencies"
    - "Onboarding step transitions still animate with a fade+slide effect"
    - "No file in the codebase imports from framer-motion"
  artifacts:
    - path: "webapp/package.json"
      provides: "Clean dependencies without framer-motion"
    - path: "webapp/src/app/onboarding/page.tsx"
      provides: "CSS-based step transitions"
  key_links:
    - from: "webapp/src/app/onboarding/page.tsx"
      to: "tw-animate-css"
      via: "Tailwind CSS animation classes"
      pattern: "animate-in fade-in slide-in-from-right"
---

<objective>
Remove framer-motion from the project entirely and replace the single usage (onboarding step transitions) with CSS-only animations using tw-animate-css (already installed).

Purpose: Eliminates ~100 KB of JS bundle cost from a library used in exactly 1 file. Replaces with zero-cost CSS transitions.

Output: Updated onboarding page with CSS transitions, framer-motion removed from package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-performance/04-CONTEXT.md
@.planning/phases/04-dashboard-performance/04-RESEARCH.md
@.planning/phases/04-dashboard-performance/04-UI-SPEC.md

@webapp/src/app/onboarding/page.tsx
@webapp/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove framer-motion and replace onboarding transitions with CSS animations</name>
  <files>webapp/src/app/onboarding/page.tsx, webapp/package.json</files>
  <read_first>
    webapp/src/app/onboarding/page.tsx
    webapp/package.json
    .planning/phases/04-dashboard-performance/04-RESEARCH.md
    .planning/phases/04-dashboard-performance/04-UI-SPEC.md
  </read_first>
  <action>
**Per D-07, D-08:** Remove framer-motion entirely and replace with tw-animate-css classes.

### Step 1: Remove framer-motion from package.json

```bash
cd webapp && pnpm remove framer-motion
```

### Step 2: Update onboarding/page.tsx

The file has 7 `motion.div` wrappers (one per onboarding step) and 1 `AnimatePresence` wrapper. Each step currently uses:

```typescript
<motion.div
  key="stepN"
  initial={{ opacity: 0, x: 20 }}
  animate={{ opacity: 1, x: 0 }}
  exit={{ opacity: 0, x: -20 }}
>
```

**Replace the import line (line 5):**
- Remove: `import { motion, AnimatePresence } from "framer-motion";`
- No new import needed — tw-animate-css classes are available globally via Tailwind

**Replace the `AnimatePresence` wrapper (line 240):**
- Remove `<AnimatePresence mode="wait">` opening tag
- Remove `</AnimatePresence>` closing tag
- The step conditionals (`{step === 1 && (...)}`) remain as-is — they control which step renders

**Replace each `<motion.div>` with a plain `<div>` with CSS animation classes:**

For ALL 7 steps, replace:
```tsx
<motion.div
  key="stepN"
  initial={{ opacity: 0, x: 20 }}
  animate={{ opacity: 1, x: 0 }}
  exit={{ opacity: 0, x: -20 }}
>
```

With:
```tsx
<div
  key="stepN"
  className="animate-in fade-in slide-in-from-right-4 duration-200"
>
```

The 7 steps are at approximately:
- Step 1 (line ~243): `key="step1"` — Objetivo
- Step 2 (line ~285): `key="step2"` — Perfil
- Step 3 (line ~336): `key="step3"` — Pulso mensual
- Step 4 (line ~411): `key="step4"` — Dashboard setup
- Step 5 (line ~497): `key="step5"` — Dashboard preview
- Step 6 (line ~559): `key="step6"` — Completion/success
- There may be additional steps — replace ALL `motion.div` instances

Each `</motion.div>` closing tag becomes `</div>`.

**Per D-08 and UI-SPEC:** Exit animations are deliberately dropped. The simplified approach (enter-only fade+slide, immediate unmount on step change) is acceptable. Duration is 200ms.

The `key` prop on each div is important — it triggers React remount when the step changes, which re-triggers the CSS `animate-in` animation.

### Step 3: Verify no other framer-motion imports remain

Run: `grep -r "framer-motion" webapp/src/` — must return nothing.

### Step 4: Run build

```bash
cd webapp && pnpm install && pnpm build
```

Must pass clean. The only file that imported framer-motion was onboarding/page.tsx.
  </action>
  <verify>
    <automated>cd webapp && pnpm remove framer-motion && pnpm install && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `webapp/package.json` does NOT contain the string `framer-motion`
    - `webapp/src/app/onboarding/page.tsx` does NOT contain `import.*framer-motion`
    - `webapp/src/app/onboarding/page.tsx` does NOT contain `motion.div` or `AnimatePresence`
    - `webapp/src/app/onboarding/page.tsx` contains `animate-in fade-in slide-in-from-right-4 duration-200` on at least 6 div elements
    - `grep -r "framer-motion" webapp/src/` returns zero results
    - `pnpm build` passes clean (exit code 0)
  </acceptance_criteria>
  <done>framer-motion removed from dependencies, onboarding step transitions use CSS animate-in classes, build passes clean, no framer-motion imports anywhere in codebase</done>
</task>

</tasks>

<verification>
1. `grep "framer-motion" webapp/package.json` returns nothing
2. `grep -r "framer-motion" webapp/src/` returns nothing
3. `grep -c "animate-in fade-in" webapp/src/app/onboarding/page.tsx` returns at least 6
4. `cd webapp && pnpm build` passes clean
</verification>

<success_criteria>
- framer-motion package completely removed from project
- Onboarding step transitions animate with CSS fade+slide (200ms)
- No file in codebase references framer-motion
- pnpm build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-performance/04-02-SUMMARY.md`
</output>
