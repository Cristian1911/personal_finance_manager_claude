# Quick Plan

- Focus: align `Ajustes` and `Analytics` with the redesigned `Más` workspace hierarchy
- Goal: make `Ajustes` feel like the control room for profile and system trust, and `Analytics` feel like an internal operating console instead of a raw dump
- Constraints: preserve the existing profile and bug-report flows, avoid changing analytics queries or schema assumptions
