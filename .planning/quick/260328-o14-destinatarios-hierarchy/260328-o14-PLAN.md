# Quick Plan

- Focus: align `Destinatarios` with the redesigned workspace hierarchy
- Goal: make merchant rules feel like a reusable system, not a flat CRUD screen
- Constraints: preserve the existing suggestions/detail flows and fix the broken create CTA without introducing a large new route structure
