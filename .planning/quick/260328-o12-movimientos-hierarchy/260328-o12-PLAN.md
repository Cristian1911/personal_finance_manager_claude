# Quick Plan

- Focus: align `Movimientos` with the new primary workspace hierarchy
- Goal: make `/transactions` feel operational and intentional instead of just a table with controls
- Constraints: preserve the existing FAB on mobile, avoid large data-layer changes, keep `Plan` as the strategic destination
