# Quick Plan

- Focus: align `Cuentas` and `Importar` with the redesigned workspace hierarchy
- Goal: make `Cuentas` feel like a control surface for the financial base, and `Importar` feel like the reset/update workflow for that base
- Constraints: preserve existing account CRUD and import wizard logic, reuse the recent shared navigation and card patterns, avoid deep rewrites of step internals
